# Raw FITS 헤더 개정에 따른 MEF ICD · MEF Converter 개정 및 검토 사항

**v1.0** · 2026-09-23 · **converter v2.5.0 재대조 + `EQUINOX` 실수형** (raw spec v1.14 동반) — 판 번호는 v0.11 대신 **v1.0** 으로 올리고 *(Draft)* 를 뗐다 (운영자 2026-09-23).  ⭐ LEECU 의 converter **v2.5.0**(D-023, 2026-09-23)이 이 문서의 C-항목 여럿을 닫아서 §1 전 행을 코드와 다시 맞췄다: ✅ C-11 · C-17 · C-18 · C-5/C-13(단 멈추지 않고 경고만 남긴다) · `UNIQNAME` 폐지 · `ORIGIN` 상수 · `EXPID`·HK·설정 포인터 중계 · `UT`·`DARKTIME` 파생 / ⚠️ **C-12 는 커밋 제목에 올랐지만 코드가 그대로다**.  **§1.2 기존 C-항목 현황 신설** · ⭐ **§7 MEF converter 및 PIPELINE 판단 필요 항목 신설** — 반쪽 pair 성한 절반의 보관(D-001 과 맞물린다)과 raw 가 공급하지 않는 MEF 카드의 처분을 새로 올리고, §1 · §1.2 · §4 · §6 에 흩어져 있던 판단 요청을 이 절로 모았다(원래 자리에는 포인터만 남겼다) · C-신설 둘(`CAMVER` 중계 · 카드 값 하드 실패의 조건) · 기록 행 다섯 신설(**`EQUINOX` 실수형** · **KASI 관측일 +9 h** · **`DATE-OBS` = 적분 개시** · **`CTRLnID`/`CTRLnSN`·`RDMODE` 미설정 = `'NC'`** · **raw `CHECKSUM`/`DATASUM` 미도입 확정**) · 틀린 서술 정정(`UT` *"시각부가 빈다"* · Radionode *"최대 600 s"* · `find_pair()` · `MIDOSC*` · `ORIGNAME` · 반쪽 pair *"확인 필요"*) · 운영자 결정 반영(게이지 Off 직후는 **다음 HK 바퀴까지 마지막 실측값** — 2026-09-14·09-15 · guide `TRIGOUT` 판정 창 = **그 프레임의 노출 창 ± `g`**(기본 0.15 s, §1.1 2행 — 2026-09-24)) · §2 대응표를 v2.5.0 기준으로 다시 대조 · §3·§4 를 ICD **v4.3** · 정의서 **v1.1** 기준으로.  ⚠️ 이 판의 요청은 converter 동작 변경보다 **문면 정정이 많다** — 우리 쪽 틀린 서술 둘(`UT` · `600 s`)이 LEECU 산출물로 옮겨 적혔다.  구판 v0.10 = 2026-09-12 · v0.9 = 2026-09-04 · v0.8 = 2026-08-30 · v0.7 = 2026-08-29 · **Part 1** = 헤더 카드 개정이 MEF 쪽 3자(ICD **v4.3** · MEF keyword 정의서 **v1.1** · converter **v2.5.0**)에 요구하는 개정 목록 · **Part 2** = raw 파일 번호 · 정체성 · 충돌 처리 재설계와 그 MEF 파급

> 구판 **v0.10 (Draft)** · 2026-09-12 · **돔 방위 셋 출처 · `OBSTYPE` 어휘 · HK 완료형 정정** (raw spec v1.13 동반) — 기록 행 둘 신설(**돔 방위 셋 = 돔 제어 프로그램 redis**, **`OBSTYPE` 이 계통 식별로**)  · C-신설 HK 히터 넷 행의 *"구현 전에는 항상 `'NC'`"* 를 **배선 완료**로 고치고 **`HKUDATE` 가 Radionode 3장의 나이를 말하지 않는다**는 경고를 더했다 · Part 2 머리말에 **저장 안 된 프레임은 번호를 소비하지 않는다**(raw spec 2.3절 8항 · D-022) · `PRESCNX` 행에 guide 값(**16**, dark reference columns)과 확인 요망 10 종결 · ICD **v4.2** · converter **v2.4.0** 표기.  ⭐ **셋 다 converter 동작 불변** — 값 공급 계통과 사실 서술의 변경이다.  구판 v0.9 = 2026-09-04 · v0.8 = 2026-08-30 · v0.7 = 2026-08-29 · **Part 1** = 헤더 카드 개정이 MEF 쪽 3자(ICD **v4.2** · MEF keyword 정의서 v1.0 · converter **v2.4.0**)에 요구하는 개정 목록 · **Part 2** = raw 파일 번호 · 정체성 · 충돌 처리 재설계와 그 MEF 파급

> **⚠️ main 영향 검토 (2026-08-30, raw spec v1.9 발행 후 전수 재검 — 제자리 추기)** — `raw_fits_spec/` 밖에서 확인된 파급과 처리 상태:
>
> - ✅ **main 에서 고쳤다** (커밋 `58bf083`): ① `project_management/governance/DECISION_LOG.md` **D-020** 의 "main 의 규격은 아직 구 문장이다" — `41845da` 합류로 거짓이 된 서술을 정정 ② 같은 문서 **D-016** 의 통합문서 현행 참조 `v0.7` → `v0.8` ③ **루트 `README.md`** 의 raw_fits_spec 설명 — pair 전용 → guide 포함 (2곳).
> - 📌 **보고만 — 각 소관에서 처리한다** (다른 작업자의 main 작업물을 이 자리에서 고치지 않는다 — 운영자 2026-08-30):
>   1. **ICD v4.1 (`../mef_fits_spec/`) 의 죽은 경로 참조** — `raw_fits_spec/KMT_CEU_Raw_FITS_Pair_Spec_v1.2.md` 는 2026-08-22 개명 이후 존재하지 않는 경로다 (현행은 `KMT_CEU_Raw_FITS_Specification_v1.9.md`). 이번 v1.9 변경과 무관한 **기존 결함**이며, ⚠️ **LEECU — 다음 ICD 개정 때 참조 갱신 요청** (이 문서 Part 1 전달분에 포함).  ✅ **ICD v4.3 (2026-09-23) 에서 정정됐다** — `…Specification_v1.13.md` 로 바뀌었다.  ⏳ raw spec v1.14 발행으로 그 v1.13 파일도 `archive/` 로 갔으므로 **v1.14 경로로 한 번 더 갱신 요청** (§3).  `mef_fits_spec/README.md` 에는 같은 죽은 경로가 아직 남아 있다(§3).
>   2. **gmon**: 프레임 기하는 규격 9장과 **완전 일치** (4224×1033 · 채널 528 = 16+512 · y_trim 9). 유일한 어긋남은 **칩 순서**(견본 `CHMAP`/`IMGROT` = N·E·S·W vs `gmon.conf` 잠정 n,s,e,w) — 규격 **OI-21** 로 등재됐고, `gmon/DESIGN.md` 에 규격 9.2/9.4 포인터를 넣는 것은 gmon 세션 몫.
>   3. **9/1 회의 아젠다 부록**의 "raw 규격 v1.7" 은 "2026-08-28 기준" 이 명시된 스냅샷이라 거짓이 아니다 — 회의 전에 기준일째 갱신할지만 판단하면 된다.
>   4. DECISION_LOG **D-020 상태줄** "main 트리에는 `siteid.py` 가 아직 있다" 는 확인 결과 **여전히 사실** — 유지 (ics_sim 합류 대기 서술).

> **v0.8 개정 (2026-08-30)** — **환경 센서 장치명 `Tapaculo` → `Radionode`** (운영자 지시 2026-08-30, raw spec v1.9 동반). 출처 표기 `Tapaculo sensor` → `Radionode sensor`, 본문 표기 전량 교체 — 장치는 같고 이름만 바뀌었으므로 **C-항목·판단 요청 내용에는 변화가 없다.** 구판(`archive/`)의 `Tapaculo` 는 같은 장치다. ⚠️ raw spec v1.9 가 **guide raw FITS 를 9·10장으로 신설**했지만 guide 는 converter·MEF 경로를 타지 않으므로(소비자는 `gmon`) **이 문서에 새 C-항목은 없다.**

> **제자리 개정 (2026-08-25) — raw spec v1.5 5장 검토 라운드 반영.** 판 이름은 그대로 두고 내용만 보강했다(참조 안정성 — raw spec·README 가 이 파일명을 가리킨다). 반영: ① **D-017 사이트 코드** — `TESTBED`/`KMTT` 폐지, `KASI`/`KMTK`. §1 의 철회됐던 `OBSERVAT` 항목을 **C-재개** 로 되살렸고 §3 의 "파일명 체계 불변" 기술을 정정했다 ② **D-018 번호 공간** `000000`–`999999`(Part 2 §2·§3) ③ **`CHMAP_*` 토큰 3자→4자** `<chip><A\|D><nn>`(C-11 · §2 — **구 3자 파서는 고쳐야 한다**) ④ **HK 카드 4장 폐지**(`AIR_IN`/`AIR_OUT`/`GLYC_IN`/`GLYC_OUT`) — §1 HK 항목 ④ · §4 신설 ⑤ **`IMGSEC` `B-BOT`→`D-BOT`**(OI-17 잔여 ①·② 종결, §5) ⑥ **`TELESCOP`/`FPAID` 사이트별 상수표**(D-017 항목 6, §4) ⑦ OI-13 재질의 3초→1초.
>
> **제자리 개정 (2026-08-26) — raw spec v1.6 반영.** ⑧ **`ORIGNAME` 폐지 · `EXPID` 신설** (운영자 확정).  값이 `<SITE>.<YYYYMMDD>.<NNNNNN>` 으로 **`DETID` 필드가 없어 pair 양쪽에서 같다** — 5.9절 "반드시 상이" 가 **7장 → 6장**이 됐다.  **converter 파급 둘**: ⓐ `ORIGNAME` 을 읽던 자리는 `EXPID` 로 옮긴다 ⓑ 충돌 판별이 "두 값 직접 비교" 에서 **`FILENAME` 의 `DETID` 필드(`.MK`/`.NT`)를 뗀 뒤 비교**로 한 단계 는다.  **얻는 것**: `EXPID` 가 pair 양쪽에 같은 값이라 **짝 탐색을 파일명 파싱 없이 이 카드 하나로** 할 수 있다(폐지된 `PAIRFILE` 이 하려던 일).  ⑨ `FILENAME` comment 개정.
>
> **v0.6 에서 바뀐 것 — raw spec 발행에 따른 정합 (2026-08-22).** ① 재작성판 **`KMT_CEU_Raw_FITS_Specification`(raw spec)** 이 발행되어 구 규격 참조를 전부 현행판으로 교체했다 — 이후 운영자 1~4장 검토 반영판 **v1.4** 로 갱신(X overscan `RRRRLLLL` 확정 → §3·§5 의 4:4 vs 5:3 항목 종결). ② **Part 2 를 파급 요약으로 축약** — 번호·충돌·정체성의 정본이 raw spec 2.3절과 DECISION_LOG **D-016** 으로 옮겨졌으므로, 본문(§1~§5·§8)을 걷어내고 MEF/구현 파급(구 §6·§7)만 남겼다(내용 이중화 방지). 전신 v0.5 는 `archive/`.

> **v0.5 에서 바뀐 것 — 두 문서를 하나로 합쳤다 (운영자 지시 2026-08-22).**
>
> 1. **통합**: `KMT_CEU_Raw_Header_Review_MEF_Impacts_v0.4.md`(→ Part 1)와 `KMT_CEU_Raw_Numbering_and_Identity_v0.2.md`(→ Part 2)를 이 문서로 합쳤다. 두 전신은 `archive/` 로 옮긴다. Part 를 가른 이유: **Part 1 은 LEECU 가 실행할 항목**이고 **Part 2 는 raw 쪽 규격 결정(+일부 MEF 파급)** 이라 주인이 다르다.
> 2. **구 검토 문서의 잔여 안건 이관**: 미결 4건(`NCTRL` 정의 · `CTRLID` 개칭 · `SATURAT`/`SATLEVEL` 통일 · `DATASRC`/설정 포인터의 MEF 목적지)을 Part 1 §6 으로 옮겼다 — 이로써 살아있는 내용은 전부 이 문서와 Header_and_Refs 로 흡수됐다.
> 3. **Header_and_Refs v1.11 반영**: 돔 Source 변경(`AUX relay` → `TCS relay or REDIS`, `DALTERR`/`DAZERR` = `ICS calculation`) · `LEDFLASH` 단위 변경([seconds] → [milliseconds] 정수) · `ICSBUILD` 형식 변경(프로그램명 제거) — 셋 다 converter 동작에는 영향이 없어 **기록 행**으로 남겼다(§1 끝).

> **v0.4 에서 바뀐 것 — 운영자 3차 개정(Header_and_Refs v1.9) 반영**: ① **`READMODE` 값 충돌 종결** — raw 쪽은 **`RDMODE` 로 개명 도입**(독출 속도 모드 선언), MEF `READMODE`(`'64AMP'`)는 그대로. ② **raw 신설 카드 `CAMVER` · `RDMODE` · `C1_`/`C2_` 계열** — converter 미독, 도입 시 pass-through 후보로 §2 대응표에 추가. ③ **C-후보 신설** — MEF `VOLTINFO`/`TELEMETRY` 를 raw `Cn_VOLT`·`Cn_CURR`·`Cn_TEMP` 에서 채우는 안. ④ **raw 미도입 확정 반영** — `CTRLTAG`·`PAIRFILE`·`OSCNPATT`·`RDDIRT`/`RDDIRB`·`MIDOSC*`·전압 색인 계열: C-5/C-12 문구를 "규격 조항 + 표본 검증" 기반으로 조정.
>
> **v0.3**: HK 재구성 확정(`WALLBOAR`→`WALLBRD`, 출처 3계통) · C-신설 2건(MEF `UT` 조립 원천 · `DARKTIME` 공급원). **v0.2**: `OBSERVAT` 값 재정의 C-항목(이후 철회로 종결). **Part 2 의 전신 이력** — v0.2: `CTRLTAG`·`PAIRFILE` 미도입 확정 반영(삼총사 문구에서 `CTRLTAG` 제거) · v0.1: 충돌 번호 증가 설계 최초 기록.

> `mef_converter/` 는 읽기 전용(LEECU 소관)이므로 Part 1 은 **변경 요청 목록**이지 변경 자체가 아니다. raw 쪽 근거는 [`KMT_CEU_Raw_FITS_Header_and_Refs_in_MEF_Converter_v1.20.md`](KMT_CEU_Raw_FITS_Header_and_Refs_in_MEF_Converter_v1.20.md)(확인 요망 11건 전량 종결 + 판정 준거 0장)와 Part 2(**D-016 등재 완료**), 검토 세션 기록([`SMC_CLAUDE.md`](SMC_CLAUDE.md))이다. raw spec v1.3 발행에 따라 v0.6 으로 판을 올렸다.

---

# Part 1 — 헤더 카드 개정에 따른 MEF ICD · 정의서 · Converter 개정 사항

## 1. Converter 변경점(C-*) 신설 · 개정

| 항목 | 내용 | 판단 요청 |
| --- | --- | --- |
| **C-신설: MEF `UNIQNAME` 공급원** | raw `UNIQNAME` 폐지 후 converter v2.4.0 까지는 `v("UNIQNAME","")` 가 **항상 빈 문자열**을 MEF 에 실었다(오류 없음) | 대안 (a) raw `FILENAME` 카드에서 채움 (b) 디스크 파일명(`mk_path`)에서 파생 — 이미 AMPINFO `RAWFILE`이 같은 원천을 씀 (c) MEF `UNIQNAME` 자체를 폐지 — MEF `FILENAME` · `RAWFILE`로 충분. **raw 쪽 권고: (c) 검토, 최소 (b)**.  ✅ **converter v2.5.0 반영 — 대안 (c)**: MEF `UNIQNAME` 카드를 폐지했다(`primary_cards()` 에 폐지 주석만 남는다 · 정의서 v1.1 `Retired` — 정체는 `FILENAME` + `EXPID`, D-016·D-019) |
| **C-재개: `OBSERVAT`·`<SITE>` 사이트 코드 (D-017, 2026-08-25)** | v0.2 에 등재됐다가 2026-08-21 에 **철회·종결**됐던 항목이 **되살아났다.** 운영자가 `TESTBED`/`KMTT` 를 폐지하고 **`KASI`/`KMTK`** 로 확정했다 — `OBSERVAT` ∈ {`CTIO`,`SSO`,`SAAO`,`KASI`}, 파일명 접두어 ∈ {`KMTC`,`KMTA`,`KMTS`,`KMTK`}. `ics_sim` 반영 완료(`rawpair.OBSERVAT`·`ORIGIN_OF`·`KASI_SITE`·보정표·`config._SITE_TELID`·~~`siteid.BENCH_SITE`~~ — ⚠️ **`siteid.py` 는 그 뒤 브랜치 `ics-archon-v1.0-build` 에서 삭제됐다**(D-015 IP 판정 폐기 → **D-020**, 2026-08-24. 사이트 판별은 `[node] observatory` 한 줄이다). **`main` 트리에는 아직 남아 있다** — 이 문서 머리말 ④ 와 같은 사실이다). | ✅ **converter 반영 완료 (2026-09-04, v2.4.0 · LEECU)** — 파일명 정규식 넷째 대안 `KMTK`, `SITE_PREFIX`/`OBS_PREFIX` `KMTK`/`KASI`, L0 MEF prefix `kmtk`. **ICD 도 v4.2 로 판올림**(§2.1 표·개정 이력, 구 v4.1 은 `archive/`). ⚠️ 이 표시는 원래 **v0.8 에 제자리로** 달렸는데 (커밋 `f4c7e5e`) v0.9 판올림이 v0.8 을 `archive/` 로 보내 고아가 된 것을 옮겨 온 것이다 |
| **C-신설(경미): MEF `ORIGIN` 을 상수로** | `ORIGIN` 개념이 **"이 파일이 생성된 곳"** 으로 확정됐다: 관측소 raw = 관측소 이름 · 테스트베드 raw = `KASI` · **KASI 파이프라인 산출물 = `KASI`**. converter v2.4.0 까지는 raw `ORIGIN` 을 MEF 로 복사했다(`v("ORIGIN","KASI")`) — MEF 는 파이프라인 산출물이므로 개념과 어긋난다 | MEF PRIMARY 의 `ORIGIN` 을 복사 대신 **상수 `'KASI'`** 로 기록. 한 줄 수정, 긴급도 낮음(관측소 raw 를 KASI 서버에서 변환하는 현행 흐름에서만 차이 발생).  ✅ **converter v2.5.0 반영** — PRIMARY `ORIGIN` 은 상수 `'KASI'` 이고 raw 값을 읽지 않는다(`primary_cards()`).  ⏳ 정의서 v1.1 의 `ORIGIN` 행은 아직 *"From raw"* 다 (§4) |
| C-신설(선택): `EXPID` pass-through | 충돌 신호(`FILENAME` 의 `DETID` 필드를 뗀 값 ≠ `EXPID`)는 raw에만 있다. MEF 층 충돌 필터가 필요할 때만 추가. **v1.6 개정 — 구 `ORIGNAME`** | raw 헤더 층 필터가 기본이므로 필수 아님.  ✅ **converter v2.5.0 반영** — PRIMARY `EXPID` 로 중계한다.  ⭐ 덧붙여 **pair 양쪽 `EXPID` 가 둘 다 있고 서로 다르면 `ValueError` 로 멈춘다**(`convert()` — 한쪽이 비었으면 검사하지 않는다).  카드 값 하드 실패가 하나 늘었다 (아래 *"카드 값 하드 실패의 조건"* 행) |
| **C-11 개정** | ⚠️ **토큰 폭이 3자→4자로 바뀌었다 (2026-08-25, raw spec v1.5)** — `<chip><A\|D><nn>`(01–08=`A`·09–16=`D`). 구 3자(`M16`)를 파싱하는 코드는 고쳐야 한다. amp `MODULE`/`CHANNEL` 공급원: 구 규격의 `AMOD<nn>`/`ACHN<nn>` 색인형 65장 → **`CHMAP_LT`/`CHMAP_LB`/`CHMAP_RT`/`CHMAP_RB` 4장**으로 재설계됐다. converter v2.4.0 까지의 추정식(`MODULE=1+((amp-1)//8)`, `CHANNEL=1+((amp-1)%8)`, 'placeholder' 주석)은 실배선(CCD 출력 채널이 chip당 1–16, TOP/BOT 대역이 chip마다 반대)과 달랐다 | `XTALKGROUP` 파생도 이 값 기준으로 재정의. `AMPMAP` 선언 카드는 폐지 방향.  ✅ **converter v2.5.0 반영** — `CHMAP_*` 4자 토큰에서 amp 정체를 유도해 amp 헤더·AMPINFO 에 `CTRUNIT`·`CCDPORT`·`CHANNAME`·`CHANNUM`·`IMGSEC`·`CHMAPSRC` 를 싣는다.  `MODULE` = 컨트롤러 포트(`A`→1 · `B`→2), `CHANNEL` = CCD 출력 채널 1–16, `XTALKGROUP` = `'<CTRUNIT>-<CCDPORT>'` 다.  내장한 `Detector_Ch_to_AmpID_Map_v1.1` 사본과 대조해 PRIMARY `CHMAPOK`·`AMPIDMAP` 에 결과를 남긴다(어긋나면 `CHMAPOK=F` + 경고·`HISTORY`) |
| C-5 · C-13 개정 | "raw geometry 선언 카드 대조" → **포장 규범 조항 + 표본 검증** 체계로 재조정 — `OSCNPATT` 는 raw **미도입 확정**(v1.9), `ROWORDR` 와 함께 규격 조항으로 이관. 대조표에 2장의 이름 대응을 명시 | ✅ **converter v2.5.0 반영 — 단 멈추지 않는다**: `check_raw_geometry()` 가 geometry 선언 12장(`NAXIS1`/`NAXIS2`·`AMPNAX1`/`AMPNAX2`·`IMAGEX`/`IMAGEY`·`PRESCNX`/`PRESCNY`·`OVRSCNX`/`OVRSCNY`·`NAMPDET`·`NAMPRAW`)과 실수 넷(`PIXSCALE`·`PIXSIZE`·`CCDXBIN`·`CCDYBIN`, 허용오차 대조)을 자기 상수와 견준다.  어긋나면 `ConverterWarning` 과 PRIMARY `HISTORY` 를 남기고 변환은 계속한다(`-W error` 로 돌릴 때만 실패).  ⚠️ 정수 sentinel `-1` 은 불일치로 걸리지만 **카드가 없으면 대조 없이 지나간다** — 금지 카드를 비운 결함은 raw 쪽 검사(raw spec 7장 체크리스트 3·8번)가 드러낸다.  v1.2 C-13 의 `DETSIZE`·`COLGAP`·`ROWGAP` 은 raw 가 싣지 않으므로 대조 대상이 아니다 (§1.2) |
| C-12 | amp `READDIR` 공급원 후보였던 `RDDIRT`/`RDDIRB` 는 raw **미도입 확정**(v1.9) — 대조 근거를 카드가 아니라 **규격 조항 + 표본 검증**으로 갱신. OI-3(실기 확인) 유지 | ⚠️ **converter v2.5.0 커밋(`3e82467`) 제목 · docstring 머리 · D-023 관련 목록은 C-12 를 v2.5.0 이 다룬 항목으로 올렸지만 코드는 그대로다** — amp `READDIR` 는 여전히 `"-Y" if amp <= 8 else "+Y"` 이고 comment 도 *"physical readout direction placeholder"* 다(`amp_header()`).  바뀐 것은 논거뿐이다 — docstring 이 raw spec 4.3절(두 축 모두 CCD 오름차순 저장)을 근거로 독출 방향이 저장 순서에 닿지 않는다고 보고 `CD` 를 뒤집지 않는다고 적었다.  정의서 v1.1 `READDIR` 행도 *"Placeholder allowed"* 이고 ICD v4.3 §12 도 placeholder 로 적는다.  C-12 를 닫을지는 **LEECU 판단 — §7-4**.  OI-3(실기 확인)은 어느 쪽이든 유지 |
| **C-신설: HK 온도 카드 재구성** (2026-08-21) | 온도센서 구성 변경으로 raw 의 Camera System House Keeping 블록이 재편됐다 — **신설 `DMPTEMP`(DMP 온도) · `WALLBRD`(wallboard 온도 — v0.2 의 `WALLBOAR` 에서 개명) · `HEBOX`(HE box 내부 온도)**, `AIR_IN`/`AIR_OUT`/`GLYC_IN`/`GLYC_OUT` comment 정의 확정(AIR는 열교환기 기준 — IN이 따뜻한 쪽, 레거시 의미 유지), `DEWPRES` 단위 [torr] · 포맷 `x.xxe-x` · **측정불가 sentinel `9.99e-9`**(값 0/이상값/게이지 비숫자 — 규격 5.0절 sentinel 표에 `DEWPRES` 전용 예외로 **등재됐다**), **`CCDTEMP` 의미 변경**: 구 설계(`CCDTEMP1`·`CCDTEMP2` 평균 파생, D-013)에서 **실측 센서 1개 값**(comment `CCD temperature` — chip 귀속 `M` 은 2026-08-30 제거)으로. `RTD12` 폐지는 확정대로(D-013). **출처 확정(v0.3)** — `CCDTEMP`·`DEWPRES`·`PT30N*`·`CHARCOAL`·`DMPTEMP`·`WALLBRD` = ICG RTD measurement / `AIR_*`·`GLYC_*` = standalone RTD readout unit / `HEBOX` = Radionode sensor | ① ~~converter 가 `DMPTEMP`/`WALLBRD`/`HEBOX` 를 읽지 않음 — MEF 로 보내려면 읽기 추가~~ → ✅ **converter v2.5.0 반영** — 셋 다 PRIMARY 로 문자열 그대로 중계한다(`primary_cards()` HK 블록) ② MEF/L1 의 `CCDTEMP` 정의를 "평균 파생"에서 "대표 센서 실측"으로 갱신 (L1 `CARRY_KEYS` 가 `CCDTEMP` 이름을 요구하므로 이름은 불변) ③ `CCDTEMP1`/`CCDTEMP2` 후보는 **제외 확정**(운영자, 2026-08-21) — 평균 파생 설계 폐기에 따름 ④ ⚠️ **추가 (2026-08-25): `AIR_IN`·`AIR_OUT`·`GLYC_IN`·`GLYC_OUT` 4장이 raw 에서 폐지됐다** (운영자 확정, raw spec v1.5 — 5.6절 18장→14장, 5.10절 폐지 목록 등재). `mef_fits_spec/KMT_CEU_MEF_FITS_Main_Keywords_Final_v1.0.md` Thermal/dewar 행이 아직 넷을 싣고 있고 converter 도 pass-through 한다. standalone RTD 계통이 raw 헤더에서 완전히 비었다.  ⏳ **정의서 v1.1(§4.8 Thermal/dewar 행)과 converter v2.5.0 에서도 그대로다** — converter 는 넷을 빈 값으로 계속 싣는다(2026-09-23 재확인).  MEF 에서의 거취는 **LEECU 판단 — §7-2** ⑤ ⚠️ **추가 (2026-09-06): 한계 밖·미연결 실측값이 걸러지지 않고 그대로 온다** (raw spec 5.0절 *"실측값은 버리지 않는다"*, 운영자 확정). 온도 카드는 ACF 센서 한계를 넘어도, 채널이 미연결이어도(≈`'-273.20'`) 값이 실리고 sentinel `'-999.99'` 는 **장치가 값을 주지 않은 자리**에만 온다. ⛔ **하류가 이 값을 정상 측정으로 세면 안 되고, 반대로 sentinel 로 접어 버려도 안 된다** — 한계 밖을 감추면 히터 과열 같은 이상 상태가 센서 고장으로 위장된다(그래서 걷은 규정이다). `CCDTEMP` 는 L1 `CARRY_KEYS` 가 이름으로 실어 나르므로 **MEF/L1 쪽 품질 판정이 이 성질을 알고 있어야 한다** — LEECU 확인 대상. ⚠️ `DEWPRES` 만 예외로 범위 밖을 `'9.99e-9'` 로 접는다(위 항목 · C-신설 게이지 Off 행) |
| **C-신설: HK 히터 넷 · `HKUDATE`** (2026-09-04, raw spec v1.10) | raw HK 블록에 **5장이 늘었다** — `HKUDATE`(**guide 유닛 실측값 중 가장 오래된 표본시각**, 19자) 와 듀어 히터 넷 `HTREN`·`HTRSET`·`HTROUT`·`HTRFORCE`. ⭐ `HKUDATE` 의 셈은 `ICG RTD` 7장 + `ICG heater` 넷만이고 **`Radionode` 3장(`HEBOX`·`FSATEMP`·`FSAHUM`)의 표본시각은 넣지 않는다**(운영자 확정 2026-09-08, raw spec 5.6절). 출처 계통 **`ICG heater`** 신설로 HK 공급 계통이 둘 → 셋이 됐다. 값은 층이 갈린다 — `RCONFIG` **되읽기**(`HTREN`·`HTRSET`·`HTRFORCE`, *명령이 아니라 컨트롤러가 받아들인 값*)와 `STATUS` **`MOD10/HEATERAOUTPUT`**(`HTROUT` — FW 1.0.1252 로 키 확인, 측정/명령값 여부 OI-28). ⚠️ `HTROUT`(0~25 V)는 guide 헤더의 `HEATER` 레일(`HEATER_V`, 공칭 +28 V)과 **다른 것**이다 | ① ~~converter 는 5장을 **읽지 않는다** — `DMPTEMP`/`WALLBRD`/`HEBOX` 와 같은 처지(위 HK 온도 항목 ①)라 **MEF 로 보낼지 LEECU 판단**이 필요하다~~ → ✅ **converter v2.5.0 반영** — 다섯 다 PRIMARY 로 문자열 그대로 중계한다.  ⏳ 정의서 v1.1 §4.8 에는 아직 없다 (§4) ② ⚠️ **sentinel 어휘가 늘었다** — HK **상태 낱말**(`HTREN`·`HTRFORCE`)과 HK **전압**(`HTROUT`)의 측정불가는 **`'NC'`** 다(온도의 `'-999.99'` · `DEWPRES` 의 `'9.99e-9'` 와 별개). 하류 파서가 `'NC'` 를 모르면 숫자 변환에서 깨진다 ③ ✅ **다섯 카드 전부 원천이 배선됐다** — `HTROUT` 은 `STATUS` 의 `MOD10/HEATERAOUTPUT`(2026-09-05), `HTREN`·`HTRSET`·`HTRFORCE` 는 HK 한 바퀴(기본 60 s)마다의 `RCONFIG` 되읽기(2026-09-09), `HKUDATE` 는 두 창구가 같은 셈을 쓴다(2026-09-06 — 2026-09-15 부터는 science 도 ICG 가 `HKDATA NOW` 답으로 준 `HKUDATE` 를 그대로 실으므로 셈하는 곳이 ICG 하나다).  실기에서 실값이 실리므로 ⛔ **하류가 *"구현 전에는 항상 `'NC'`"* 를 전제로 파서를 짜면 안 된다** — `'NC'` 는 되읽기가 실패했거나(ACF 적용 중·파싱 전) 표본이 낡은 구간의 표시다 (raw spec 5.6.2절 · 구 ~~OI-25~~ 종결).  ⏳ 값의 **뜻**(출력단 측정값이냐 명령값이냐)만 아직 미확정이다 — raw spec **OI-28**  ④ ⛔ **`HKUDATE` 로 `HEBOX`·`FSATEMP`·`FSAHUM` 의 나이를 판단하면 안 된다** — 그 셋(`Radionode` 계통)은 시각 셈에서 빠지고, 그 신선도는 폴러의 창이 따로 보증한다 — **장치 전송주기 ×3** 까지 낡을 수 있다(600 s 장치면 **1800 s**, 주기를 배우기 전에는 INI 초기값 **4000 s** · `local_lns` 백엔드는 주기를 배우지 못해 INI 값이 그대로 창이다 — raw spec 5.6절 신선도 표, 운영자 확정 2026-09-08).  ⚠️ v0.10 까지 이 자리에 적었던 *"이 카드보다 최대 600 s 더 낡을 수 있다"* 는 **틀렸다** — 600 s 는 전송주기이지 최대 나이가 아니다.  그 틀린 수가 converter v2.5.0 의 `HEBOX` 주석(*"can be up to 600 s older"*, `primary_cards()` HK 블록)에 옮겨 적혔다 — **LEECU 정정 요청**.  MEF 로 그 세 값을 올릴 때 `HKUDATE` 를 그 시각으로 쓰면 **어긋난다** — 그 셋의 나이는 raw 헤더만으로는 알 수 없고 ICG 의 HK CSV 에만 남는다 |
| **C-신설: `DEWPRES` 게이지 Off 구간** (2026-09-04, raw spec v1.10) | 진공 이온게이지 필라멘트가 science 영상에 영향을 주므로 **science 노출 중에는 게이지를 끈다**(ICS 가 `GO` 의 `HKDATA NOW` 답을 받은 **뒤** 노출 앞에 `VACGAUGE OFF`, **독출 완료** 뒤 `gauge_reenable_after`(기본 10분)에 `ON` — 셔터 닫힘 기준이 아니다). 그 구간의 `DEWPRES` 는 **`'9.99e-9'`** 이고 이는 **결측이 아니라 의도된 상태**다. ⭐ **sentinel 이 정상인 구간은 셋이다** — ① 끈 동안 ② **켠 뒤 예열 중**(`gauge_warmup_wait`, 기본 12 s — 켤 때마다) ③ **연속 노출 사이**(취득 중이면 켜지 않고 켜짐대기로 남긴다).  ⭐ **① 은 끈 순간이 아니라 다음 HK 바퀴부터다** (운영자 2026-09-14·09-15, 브랜치 DevNote 11.89·11.93) — `DEWPRES` 를 가리는 곳은 ICG 의 HK 바퀴 하나뿐이고, 끈 뒤 그 바퀴가 돌 때까지는 **켜져 있을 때 잰 마지막 값**이 남는다.  그래서 **science** 는 게이지가 켜져 있던 `GO` 의 n 장 전부에 **끄기 직전 실측값**이 실리고(그 `GO` 의 `HKDATA NOW` 답이 끄기 전에 온다), 답이 이미 `OFF`·`WARMUP` 이면 sentinel 이다.  **guide** 는 끈 뒤 다음 바퀴까지 그 마지막 값을 싣는다 — 그 `GO` 의 `HKDATA NOW` 가 주기 타이머를 밀어 두므로 대개 `NOW` 뒤 `[hk] interval`(기본 60 s)까지다.  ⚠️ v0.10 까지 이 자리의 *"끈 것을 아는 순간부터 즉시"* 는 2026-09-14 결정 전의 규칙이다.  ⚠️ 게이지 상태를 **모를 때는 막지 않는다** | ⚠️ **하류가 이 값을 고장으로 세면 안 된다** — 정상 관측분의 상당수가 그렇게 나온다. ⚠️ **구간이 셋이라 sentinel 이 노출 직후에도, 연속 노출 내내도 이어진다** — "한 노출에 한 번" 으로 가정한 탐지기는 오탐한다. ⭐ `ON` 뒤 첫 실값은 **최악 약 2분** 늦을 수 있다(예열 + 모듈 VCPU 재시작 바퀴). ⛔ **Conductron 함정**: 이온게이지를 꺼도 같은 모듈의 열손실 센서가 값을 계속 내보내고 그 값이 인정 범위 `[1e-8, 1e+3]` 를 **경고 없이 통과한다** — 끈 동안 오는 값을 실으면 *"정상으로 보이는 틀린 값"* 이 남는다.  두 헤더 모두 Conductron 값은 싣지 않는다.  guide FITS 도 같은 게이지지만 **양쪽이 동시에 sentinel 로 바뀌지는 않는다** — 끈 직후 한 바퀴 동안은 guide 헤더에 켜져 있을 때 잰 값이 남으므로, pair 와 guide 파일을 견줄 때 그 시차를 이상으로 세면 안 된다 |
| **C-신설: 반쪽 pair** (2026-09-04, raw spec v1.10 2.1절) | 두 컨트롤러가 **나란히** 프레임을 받고 각자 저장이 끝나는 대로 파일을 내며, **한쪽이 실패해도 성공한 쪽은 저장한다.** 그래서 `…MK.fits` 나 `…NT.fits` **한쪽만 있는 노출 번호가 실재**한다 — 결측이 아니라 의도된 부분 성공이다 | ⚠️ **converter 의 짝 탐색이 이 경우를 오류로 세면 안 된다** — 짝이 없는 노출은 **건너뛰되 실패로 세지 않는 것**이 규격 의도다(7장 체크리스트 2번이 경고로 내려갔다). ~~현행 짝 탐색이 어떻게 동작하는지 확인 필요~~ → **확인 결과 (2026-09-23, converter v2.5.0)**: `find_pair()` 는 파일이 있는지 보지 않고 접미사(`.MK.fits`↔`.NT.fits`)만 바꿔 끼우고, `main()` 은 짝이 없으면 **`FileNotFoundError` 로 멈춘다** — 메시지가 *"…is missing, so this exposure is a half pair; skip it rather than treating it as a failure (raw spec 2.1)"* 라서 건너뛰기를 **converter 를 부르는 쪽에 넘긴 것**이다.  그런데 그 배치 드라이버가 저장소에 없다(호출처는 단건 예제 `run_kmt_ceu_l0amp_example.sh` 뿐).  **LEECU 요청**: 밤 단위로 converter 를 부르는 배치가 이 예외를 **건너뛰기로 처리**하고 실패 집계에 넣지 않게 할 것.  건너뛴 노출의 **성한 절반을 어디에 보관할지**는 **LEECU 판단 — §7-1** (D-001 과 맞물린다) |
| **C-신설: MEF `UT` 조립 원천** (2026-08-21) | raw 가 `TSHOPEN`/`TSHSHUT` 를 싣지 않는 것으로 판정됐다(Header_and_Refs v1.9 3.2절). converter v2.4.0 까지는 `DATE-OBS` 날짜부 + raw `TSHOPEN` 으로 MEF `UT` 를 조립하되 **`TSHOPEN` 이 없으면 `DATE-OBS` 전체를 실었다**(PRIMARY·amp 두 자리 모두 — v2.1.1 부터 같은 식).  ⚠️ v0.10 까지 이 자리에 적었던 *"MEF `UT` 시각부가 빈다"* 는 **사실이 아니었다** — `TSHOPEN` 이 없는 raw 에서도 `UT` 는 밀리초까지 든 `DATE-OBS` 였다 | ~~`UT` 조립 원천을 `DATE-OBS` 의 시각부로 교체 (`DATE-OBS` 는 밀리초까지 담는다, D-014)~~ → ✅ **converter v2.5.0 — `UT` = `DATE-OBS`**(PRIMARY·amp 모두 조건 없이).  ⭐ 새 사실: raw 에 `DATE-OBS` 가 없으면 PRIMARY `UT`(와 `DATE-OBS`·`MJD-OBS`·`JD`)는 **변환 시각**, amp `UT` 는 **빈 문자열**이다 (C-6 미반영 — §1.2).  ⚠️ **LEECU 정정 요청** — 이 행의 틀린 전제가 LEECU 산출물로 옮겨 적혔다: converter docstring(v2.5.0 changes 의 Provenance 항목 *"…which left its time part blank"*) · `primary_cards()`·`amp_header()` 의 `UT` 주석 · 정의서 v1.1 `UT` 행(*"시각 부분을 비운 채 넘어온다"*).  동작은 옳으니 **문면만** 고치면 된다 |
| C-신설(경미): MEF `DARKTIME` 공급원 | raw `DARKTIME` 미기재 판정 — 값이 `EXPTIME` 과 동일해 파생으로 충분(v1.9 3.2절). converter v2.4.0 까지는 기본값 `0.0` 이 MEF 에 박혔다 | ~~`EXPTIME` 값으로 파생 기록, 또는 MEF `DARKTIME` 폐지 판단~~ → ✅ **converter v2.5.0 — `EXPTIME` 에서 파생**(`fnum(…,"DARKTIME",exptime)` · 정의서 v1.1 `DARKTIME` 행).  ⚠️ raw 에 `EXPTIME` 도 없으면 둘 다 `0.0` 이다 (C-6 과 같은 부류 — §1.2) |
| **C-후보 신설: MEF `VOLTINFO`/`TELEMETRY` 공급원** (2026-08-22) | raw 가 컨트롤러별 텔레메트리 카드 **`C1_TEMP`·`C1_VOLT`·`C1_CURR` / `C2_*`** 를 도입했다(v1.9, 구 `BCKTEMP` 확장) — MEF `VOLTINFO`/`TELEMETRY` 를 실측값으로 채울 원천이 처음 생겼다. converter v2.4.0 까지는 이 카드들을 읽지 않았다 | 현행 placeholder 경로(C-18)를 raw `Cn_*` 기반 채움으로 **대체**하는 안 — 도입 시 읽기 추가 + 조립 규칙 정의 — **모듈·레일 자리 순서 명세는 raw spec 5.6.1절에 수록됐다** (v1.5, 2026-08-25).  ✅ **converter v2.5.0 반영 (= C-18)** — `Cn_VOLT`/`Cn_CURR` 의 7 레일 × 2대 × {전압, 전류} = 28행이 `VOLTINFO` 실측행이 되고(9 → 37행 — 앞 9행 CCD bias/clock 은 공급원이 없어 `-999.0`/`'PLACEHOLDER'`), `Cn_TEMP` 1번 자리(Backplane)가 `TELEMETRY.BOARDTEMP` 다.  `Cn_*` 문자열 6장은 PRIMARY 에도 그대로 싣고, 자리 순서 판은 PRIMARY `RAILREF='RAWSPEC-v1.13-5.6.1'` 로 남긴다 — v1.14 5.6.1절의 자리 순서는 v1.13 과 같으므로 그 값은 **참**이다 |
| **C-신설: MEF `CAMVER` 를 raw 에서 중계** (2026-09-23) | raw `CAMVER` 는 범프 규범이 선 카드다(raw spec 5.2절 — 포장 4.3절 · `Cn_*` 자리 5.6.1절 · 듀어 RTD 배치 10.4절이 범프 사유이고, 셋째는 헤더 어디에도 자취를 남기지 않아서 이 카드 값만이 배치를 가른다).  그런데 converter v2.5.0 은 MEF `CAMVER` 를 **상수 `'CEU-v2.1'`** 로 쓰고 raw 값을 읽지 않는다(`primary_cards()` · 정의서 v1.1 `CAMVER` 행도 `Required` 상수 `CEU-v2.1`).  지금은 값이 같아 드러나지 않지만 **첫 범프 뒤 raw 와 MEF 가 조용히 갈린다** | MEF `CAMVER` 를 raw `CAMVER` 에서 **그대로 옮기고**, raw 에 없을 때만 기본값을 쓴다(정의서 행도 *"From raw"* 로).  ⚠️ 값↔구성 대장은 raw spec **OI-29** 다 — 그 대장이 서기 전에는 `CAMVER` 값 하나로 HW 세대 변경과 RTD 재배치를 가를 수 없다는 점도 함께 적어 둘 것 (§2 `CAMVER` 행).  ✅ 같은 구조였던 `PIXSCALE`·`PIXSIZE` 는 v2.5.0 이 raw 선언을 대조한다(허용오차 1e-6, 어긋나면 경고·`HISTORY`) — MEF 값은 여전히 상수 |
| **C-신설: 카드 값 하드 실패의 조건** (2026-09-23) | converter 가 **카드 값** 때문에 멈추는 경우는 v2.5.0 에서 **둘**이다.  ① **`OBSERVAT` ↔ 파일명 `<SITE>` 불일치** — 검사가 출력 이름을 짓는 `default_output_name()` 안에만 있어서 **기본 출력 이름 경로에서만** 돈다: `-o` 를 주면 불리지 않고, 파일명이 D-011 문법에 안 맞으면 경고만 남기고 `OBSERVAT` 쪽 접두어로 이름을 짓고, `OBSERVAT` 가 없거나(기본 `'KMT'`) 네 값 밖이면 비교 없이 지나간다.  ② **pair 양쪽 `EXPID` 불일치** — `convert()`, 둘 다 있을 때만.  파일 구조 쪽 실패(`BITPIX` · 모양 · `END`)는 따로다.  그 밖의 이상은 **경고 층**으로 간다 (§1.2) | **LEECU 요청**: `OBSERVAT` 교차검증을 출력 이름 결정과 떼어 `convert()` 에서 **조건 없이** 돌리고, `OBSERVAT` 결측·네 값 밖도 오류로 할 것 — raw spec 2.2절의 *"그 넷 밖의 값은 기동을 거부한다"*(D-020)가 취득 쪽 규칙이라면 이것은 변환 쪽의 짝이다.  그때까지 ICD v4.3 §2.1 의 *"raises an error on mismatch"* 에는 조건을 달아야 한다 (§3).  ⭐ 예제 스크립트는 이제 기본으로 `-o` 를 주지 않아 검사가 돈다 — ICD §11 예시만 `-o` 를 쓴다 |
| **(→ §7-2) raw 가 공급하지 않는 MEF 카드** (2026-09-23) | raw 가 싣지 않기로 판정된 카드인데 converter v2.5.0 이 raw 에서 읽으려다 기본값을 박는 자리 — 카드 목록과 인접 결함(`BIASVER`/`CLKVER` 두 값)은 **§7-2** | **LEECU 판단 — §7-2** |
| (기록) `DATASRC` 값 체계 확장 · chiller 블록 미기재 | `ARCHON`/`SIM` → **`ARCHON_SCIENCE`/`ARCHON_GUIDE`/`SIM`**(`HEMODE` 흡수) · chiller 4장(`CHSTAT` `CHOP` `CHSET` `CHPROC`)은 raw 미기재 **확정**(운영자가 초안에서 삭제, 2026-08-21 · 재삭제 검증 2026-08-22) | converter 는 `DATASRC`/`CHOP`/`CHSET`/`CHPROC` 를 읽지 않고 `CHSTAT` 는 기본값 `""` 경로 — 영향 없음/경미, 기록만.  ⚠️ **converter v2.5.0 부터 `DATASRC` 는 PRIMARY 로 간다**(없으면 `'UNKNOWN'` — §6-4 ① 종결).  `CHSTAT` 은 여전히 빈 값으로 실린다 (§7-2) |
| (기록, v0.5) 돔 Source 변경 · `LEDFLASH` 단위 변경 · `ICSBUILD` 형식 변경 | ① 돔 카드(`DSSTAT`~`DSTELAZ`)의 공급원이 `AUX relay` → **`TCS relay or REDIS`**(newTCS 편입), `DALTERR`/`DAZERR` 는 **`ICS calculation`** — 값 공급 계통의 변경이지 카드 이름·형식 변경이 아니다 ② `LEDFLASH` 단위 [seconds] → **[milliseconds] 정수**(운영자 확정 2026-08-22 — 카드 comment 가 단위 명시) ③ `ICSBUILD` 형식 `<프로그램>-v<버전>:<빌드일시>` → **`v<버전>:<빌드일시>Z`**(프로그램 식별은 `DATASRC` 담당) | 셋 다 converter 미독 카드(`LEDFLASH` `ICSBUILD`) 또는 pass-through 값이라 **converter 동작 불변 — 기록만**. 단 하류 도구가 `LEDFLASH` 를 초로 읽지 않게 ICD/정의서 부속 문서에 단위 변경을 전파할 것.  ⚠️ **converter v2.5.0 부터 `LEDFLASH`·`ICSBUILD` 도 PRIMARY 로 간다** — `ICSBUILD` 는 문자열 그대로(없으면 `'UNKNOWN'`), `LEDFLASH` 는 `fnum(…,0.0)` 이라 raw 정수 [ms] 가 MEF 에서 **실수**가 되고 카드가 없으면 `0.0`(*"점등 안 함"* 으로 읽히는 그럴듯한 값)이다.  ✅ 단위 전파는 정의서 v1.1 `LEDFLASH` 행(*"[ms]"*)에 반영됐다 |
| **(기록, v0.10) 돔 방위 셋 출처 = 돔 제어 프로그램 redis** (2026-09-11, D-021 · CR-003 · raw spec v1.13) | `DSAZ` · `DSTELAZ` · `DAZERR` 의 공급원이 `TCS relay or REDIS` / `ICS calculation` → **`REDIS (dome control)`** 로 바뀌었다 — 돔 제어 프로그램이 `dome_az` · `dome_tel_az` · `dome_del_az` 에 TTL 수백 ms 로 싣는 값을 ICS·ICG 가 직접 읽고, 키가 없으면 `NC` 다(옛 값을 이어 싣지 않는다). ⭐ **`DAZERR` 의 성격이 계산에서 중계로 바뀐다** — `ICS calculation`(`DSAZ`−`DSTELAZ`, −180~+180 접기)은 `dome_del_az` 만 없을 때의 예비 경로로 남는다. `DALTERR` 는 그대로 `ICS calculation`(고도차, 접기 없음) | **값 공급 계통의 변경이지 카드 이름·형·pass-through 여부의 변경이 아니다 — converter 동작 불변, 기록만.** 단 하류 문서가 `DAZERR` 를 *"ICS 파생값"* 으로 적어 둔 곳이 있으면 **실측 중계값(예비 경로만 파생)** 으로 고칠 것.  ✅ 2026-09-23 확인 — **그런 하류 문서는 없다** (§1.1 주) |
| **(기록, v0.10) `OBSTYPE` 어휘 변경** (운영자 확정 2026-09-09 · raw spec v1.13) | raw `OBSTYPE` 이 `IMAGETYP` 의 사본에서 **어느 계통이 찍었나**로 갈렸다 — science `SCIENCE` · guide `GUIDE`(`OBSTYPE` 명령으로 지정, 대문자로 접음). converter 는 이 값을 **그대로 복사**하므로 **동작 불변**이고, L1 전처리는 `OBSTYPE` 을 읽지 않는다(`IMAGETYP` 만 문자열 비교한다) | ⚠️ **기록만 — converter 수정 없음.** 다만 ① MEF Keywords 정의서 v1.0 의 `OBSTYPE` 설명 *"observation type"* 이 이제 프레임 종류가 아니다(§4 등재) ② `mef_pipeline` 의 시험 고정자료가 `OBSTYPE` 을 `IMAGETYP` 값으로 채워 **옛 규약을 재현**한다 — 하류가 두 카드를 동의어로 보지 않게 LEECU 확인.  ⏳ ① 은 정의서 v1.1 에도 *"observation type"* 그대로다 (§4) |
| **(기록, v1.0) `EQUINOX` 실수형** (운영자 확정 2026-09-23 · raw spec v1.14 5.7절) | raw `EQUINOX` 가 레거시 계승 문자열(`'2000.000'`, 결측 `'NC'`)에서 **실수 `2000.0`** 으로 바뀌었다 — 결측은 실수 sentinel **`-999.0`**.  MEF 쪽 경로(converter v2.5.0 기준): ① **MEF 형은 raw 형과 무관하게 실수다** — converter 가 스스로 `fnum(…,"EQUINOX",2000.0)` 으로 쓴다(docstring · 정의서 v1.1 `EQUINOX` 행).  PRIMARY 에는 늘 싣고, amp 에는 seed WCS 를 쓸 때만 싣는다(N·T amp 는 NT 헤더에서 읽는다).  pair 동일 카드라 MK·NT 가 다르면 경고가 난다.  ② **`-999.0` 은 그대로 간다** — `_num()` 이 유한수로 보고 통과시키므로 PRIMARY 에 실리고, `RA`/`DEC` 는 풀리는데 `EQUINOX` 만 결측인 하늘 프레임이면 amp seed WCS 의 `EQUINOX` 에도 실린다.  그 값을 `mef_pipeline` `steps/assemble.py` 의 `WCS_COPY_KEYS` 가 **L1 CCD WCS 로 복사**한다.  converter 는 `HK_REAL_SENTINEL = -999.0` 을 정의해 두고도 여기에는 적용하지 않는다.  ③ **카드가 없거나 비수치면 `2000.0`** 이다 — 결측 표지(`''` · `'NC'` · `'N/A'` 등)도 같다.  그래서 **v1.13 이전 raw 의 결측 `'NC'` 는 MEF 에서 그럴듯한 `2000.0` 이 된다.**  ④ 좌표 해에는 쓰이지 않는다 — `RADESYS` 는 raw `RADECSYS`(`'ICRS'`)를 중계하고 `CRVAL` 은 `RA`/`DEC` 만으로 정해진다.  다만 수치로 세차 보정을 하는 하류는 `-999.0` 에서 깨진다.  ⑤ **아카이브에 두 형이 섞인다** — 레거시 MEF · converter v2.4.0 이하 L0 = 문자열 `'2000.000'` / v2.5.0 이상 L0 = 실수 | ⚠️ **converter 의 형 변경은 요청하지 않는다**(이미 실수다).  **LEECU 요청 둘**: ① `EQUINOX` `-999.0` 을 **결측으로 다룰 것** — PRIMARY·seed WCS 에 수로 싣지 말고(카드 생략이나 결측 표지), L1 복사도 같이 ② 정의서 v1.1 의 `EQUINOX` 두 행(PRIMARY 행 *"raw는 문자열 `'2000.000'`으로 넘기는데"* · WCS 표 `2000.0` float 행)에 **raw v1.14 가 실수이고 결측이 `-999.0`** 이라는 것을 적고, *"raw는 문자열"* 은 v1.13 이전의 말로 고칠 것.  두 형이 섞인 아카이브를 읽는 하류(`EQUINOX` 를 문자열로 가정한 코드)도 확인 대상이다 |
| **(기록, v1.0) KASI 관측일 보정 +9 h** (운영자 2026-09-12 · DECISION_LOG D-017 항목 4 개정) | `KMTK` 파일명의 `<YYYYMMDD>` 가 **KST 날짜**가 됐다 — 관측일 보정 **+9 h**, 경계 **UT 15:00 = KST 00:00**.  관측 3사이트의 경계는 그대로 현지 12:30 이고 KASI 만 규칙이 다르다(관측 야간이 없는 실험실이라 동지 근거가 성립하지 않는다 — 벤치에서 사람이 보는 날짜와 파일 이름을 맞춘다).  ⚠️ **그 구현이 없는 코드로 찍은 KASI 파일은 보정 0(UT 날짜)** 이라 KST 09:00 이전에 찍은 것은 하루 앞선 이름을 갖는다 — 구현은 `ics-archon-v1.0-build` 의 `rawpair.py`(커밋 `982ebe5` · 2026-09-13)이고, 그 커밋 전의 브랜치와 합류 전 `main` 의 `ics_sim`(보정표 `'KMTK': 0`)은 보정 0 이다.  그래서 규칙이 갈리는 기준은 날짜가 아니라 **파일을 찍은 코드**다.  DECISION_LOG D-017 항목 4 의 *"`KMTK` 의 보정은 **0** 이다"* 는 이 결정으로 개정됐다(D-017 머리 배너 · 항목 4 인라인 표시 · 상태줄 Amended) | **converter 동작 불변 — 기록만.**  converter 는 관측일을 셈하지 않는다 — `default_output_name()` 이 파일명의 8자리(`\d{8}`)를 출력 이름으로 옮길 뿐이다.  ICD v4.3 §2.1 은 `<YYYYMMDD>` 를 *"observing-night date"* 로만 적고 경계는 raw spec 2.2절·D-017 을 인용하므로 **ICD 문면은 고칠 것이 없다** — 인용한 D-017 이 개정됐다는 것만 알린다.  ⚠️ 날짜 필드로 KASI 자료를 묶는 하류 도구가 있으면, 그 날짜가 찍은 코드에 따라 UT 날짜이기도 하고 KST 날짜이기도 하다는 것을 알 것 |
| **(기록, v1.0) `DATE-OBS` = 적분 개시** (운영자 확정 2026-09-23 · raw spec v1.14 5.4절 · 5.7.1절 (c) · 10.1절) | raw `DATE-OBS` 의 뜻을 **모든 영상(`DARK`·`BIAS` 포함)의 적분 개시 시각**으로 규범화했다 — 셔터 노출은 ICS 가 셔터 개방(`SHOPEN`)을 지시한 시각, 셔터 없는 노출은 컨트롤러 적분을 건 시각, guide 는 10.1절(트랜스퍼 개시 = 그 프레임 적분의 시작)이다.  종전 5.4절 문면(*"ICS 가 `SHOPEN` 을 지시한 UTC"*)은 셔터 노출만 말했다.  ⚠️ 현행 브랜치의 science `DARK` 는 `DATE-OBS` 를 찍은 뒤 호스트 카운트다운을 다 돌고 나서 독출 국면(`_readout_stream()`)에서 컨트롤러 적분을 건다 — 규범이 아니라 코드 쪽이 고칠 결함이다.  그 코드로 찍은 `DARK` 는 `DATE-OBS` 가 적분을 건 시각보다 약 `EXPTIME` 앞선다 | **converter 동작 불변 — 기록만.**  MEF `DATE-OBS`·`MJD-OBS`·`JD`·`UT` 는 모두 raw `DATE-OBS` 에서 나오고(converter v2.5.0), 정의서 v1.1 의 *"UTC exposure start time"* 과도 맞는다.  ⚠️ `DARK`·`BIAS` 의 `DATE-OBS` 는 셔터 사건이 아니다 — §7-2 에서 MEF `TSHOPEN`(*"shutter open time"*)의 공급원을 정할 때 `DATE-OBS` 로 채우면 셔터 없는 노출에서 뜻이 어긋난다.  guide 는 MEF 경로가 없다 |
| **(기록, v1.0) `CTRLnID`·`CTRLnSN`·`RDMODE` 미설정 = `'NC'`** (운영자 확정 2026-09-23 · `RDMODE` 는 2026-09-24 · raw spec v1.14 5.0절 · 5.5절 · 10.3절) | INI 에 컨트롤러 ID·시리얼이 없으면 raw `CTRL1ID`/`CTRL1SN`/`CTRL2ID`/`CTRL2SN`(guide 는 ICG INI 의 `CTRL1ID`/`CTRL1SN`)은 **`'NC'`** 다 — Archon `SYSTEM` 값(펌웨어 문자열 · 16진 `BACKPLANE_ID`)으로 대신 채우지 않는다.  INI 에 값이 있을 때의 형식(`'<SITE>-SCI-101'` · `'STA-nnnn'`)은 그대로다.  ⭐ **`RDMODE` 도 같다** — INI 가 비면 종전 `'UNKNOWN'`(v1.12~v1.13) 대신 **`'NC'`** 를 싣는다(raw 가 짓는 문자열 결측 낱말을 `'NC'` 하나로 통일).  ⚠️ 현행 브랜치 science 는 INI 가 비면 `SYSTEM` 값으로 채운다(`backend.controller_info()`) — 규범이 아니라 코드 쪽이 고칠 결함이다 | **converter 동작 불변 — 기록만.**  converter v2.5.0 은 네 카드를 PRIMARY 로 문자열 그대로 옮기므로(`v("CTRL1ID","UNKNOWN")` — `'UNKNOWN'` 은 카드가 없을 때만이다) raw `'NC'` 는 MEF 에도 `'NC'` 로 간다.  pair 동일 검사(`check_raw_pair()`)에도 든다.  ⚠️ MEF 하류는 `'NC'`(raw 가 미상이라고 적었다)와 `'UNKNOWN'`(raw 에 카드가 없다)을 둘 다 미상으로 다룰 것 — 규범상 펌웨어 문자열·16진 시리얼 형식의 값은 오지 않는다.  다만 현행 브랜치 코드로 INI 를 비운 채 찍은 파일에는 그 값이 실려 있으므로, 형식으로 걸러 미상에서 빼지 말 것.  정의서 v1.1 §10 이 이 카드들의 확정값 출처로 적은 *"Archon controller inventory 및 firmware report"* 는 raw 쪽에서는 INI 값으로만 들어온다 |
| **(기록, v1.0) raw `CHECKSUM`/`DATASUM` 미도입 확정** (운영자 확정 2026-09-23 · raw spec v1.14 5.10절 · 8장 ~~OI-7~~) | raw 는 `CHECKSUM`/`DATASUM` 을 싣지 않는다 — 원장 7장의 `X`(원장 v1.9 확정)와 규격 OI-7(*"도입 여부 결정 대기"*)이 갈려 있던 것을 `X` 로 맞췄다 | **converter 동작 불변 — 기록만.**  이것은 **raw** 카드의 말이다 — MEF 는 따로 싣는다: converter v2.5.0 이 모든 HDU 에 `DATASUM`/`CHECKSUM` 을 쓰고(`hdu_bytes()`), 정의서 v1.1 §11 최소 검증 Checklist 가 *"전 HDU `CHECKSUM`/`DATASUM` 검증 통과"* 를 release·archive 전 확인 항목으로 둔다.  ⚠️ 그래서 MEF checksum 이 보증하는 것은 **변환 뒤의 바이트**뿐이다 — raw 가 변환 전에 비트 오류를 입었으면 그 값이 MEF 로 옮겨져 거기서 *"검증 통과"* checksum 을 받는다.  converter 는 픽셀 바이트를 검사하지 않는다 — 손상된 raw 를 변환 앞에서 멈춰 세우는 것은 파일 구조(`BITPIX` · 모양 · `END`)가 틀릴 때이고, 상한 헤더 카드는 카드 값 하드 실패 둘(위 *"카드 값 하드 실패의 조건"* 행)이나 경고 층(§1.2)에 걸릴 때만 드러난다 |

## 1.1 raw spec v1.13 헤더 변경 — science ↔ guide 대비표 (2026-09-12)

**raw spec v1.13 이 헤더에 남긴 변경 전량이다.**  ⭐ **카드 수와 레코드 수는 양쪽 다 안 바뀐다** —
science 값 136장 · 180 레코드 · 14,400 B, guide 값 128장 · 144 레코드 · 11,520 B.
⛔ **converter 동작도 불변**이다 — 이름·형·pass-through 여부가 그대로이고 바뀐 것은
**값·출처·comment** 다.  `×` = 해당 없음.

| # | 카드 · 자리 | science (`MK`·`NT`) | guide (`G`) | 무엇이 바뀌었나 | 규격 |
| ---: | --- | --- | --- | --- | --- |
| 1 | `LEDFLASH` | **유지** (`I`, [ms]) | ⛔ **삭제** | guide 는 점검용 LED 를 쓰지 않는다 | 5.4 · 10.2 · 10.3 |
| 2 | **`TRIGOUT`** | × | ⭐ **신설** — `EXPTIME` 다음, `I`, `1` = 그 프레임의 판정 창 **`[DATE-OBS − g, t_next + g]`** 에 Trigger Out 이 HIGH 였다.  모르면 sentinel `-1`.  `t_next` 는 이 프레임의 트랜스퍼 개시(같은 블록에서 다음 장이 저장되면 그 장의 `DATE-OBS`, ≈ `DATE-OBS` + `EXPTIME` — 호스트는 다음 장을 기다리지 않고 이 프레임의 완료 관측에서 셈한다)라 창의 뼈대는 **그 프레임의 노출 창**이고, `g` 는 호스트 시각 불확도 여유다 — ICG INI `[icg] trigout_guard`, 기본 **0.15 s** (raw spec v1.14 10.3절 · 운영자 2026-09-24).  이웃 창은 **`2g` 만** 겹친다 — 경계 ±`g` 안의 펄스는 이웃한 두 장에 `1` 로 찍힐 수 있다(의도).  남는 한계 넷(에지·경계 지연 차이가 `g` 를 넘는 드문 경우의 경계 근처 거짓 `0` · 독출 중 store 로 번진 LED 는 그 프레임에 안 잡힌다 · 마지막 저장 장의 트랜스퍼 개시 + `g` 뒤 펄스는 어느 파일에도 안 남는다 · 이진 플래그라 얼마나 비췄는지는 말하지 않는다)은 10.3절.  ⚠️ raw spec v1.13 은 이 창을 *"노출 창"* 이라 부르면서 `[DATE-OBS, 독출 완료]` 로 적었고(적분이 *"이 독출로 끝난다"* 는 사실 오류 — 10.1-5 와 모순), 2026-09-23 결정은 그 넓이에서 생긴 자기 독출 ≈ 1.25 s 겹침을 *"의도"* 로 확정했다.  2026-09-24 결정이 그것을 고쳐 창을 노출 창 ± `g` 로 좁혔다 — 뼈대가 이름대로 노출 창이라 G 견본 comment *"during exposure"* 는 그대로 맞다.  ⚠️ 현행 브랜치 코드는 아직 종전 창을 쓰고 `trigout_guard` 키가 없다 — 규범이 아니라 코드 쪽이 고칠 결함이다.  그 코드로 찍은 파일의 `TRIGOUT` 은 종전 창으로 판정된 값이고 카드 값만으로는 두 창을 가르지 못한다 — 취득 SW 판(`ICGBUILD`)으로 가린다(10.3절) | `LEDFLASH` 자리를 1:1 로 대신한다 — **장수 불변** | 10.3 |
| 3 | `OBSTYPE` | 값 **`'SCIENCE'`** | 값 **`'GUIDE'`** | ⛔ `IMAGETYP` 의 **사본이 아니다** — *어느 계통이 찍었나* 로 뜻이 갈렸다.  계통 기본값이고 `OBSTYPE` 명령이 덮는다.  **견본 3장의 값도 고쳤다** | 5.4 · 10.3 |
| 4 | `INSTRUME` | 그대로 (`'<SITE> 18k CCD'`) | ⭐ **`'<SITE> Guide CCDs'`** 확정 | OI-24 잔여 하나가 닫혔다 (복수형 `s`) | 10.3 · 10.6 |
| 5 | `FPAID` | 그대로 (사이트 유도) | ⭐ **science 와 같은 사이트 유도** | guide CCD 도 FPA 조립체에 든다 — 종전의 *"귀속 확인 대기"* 가 닫혔다 | 5.3.1 · 10.3 |
| 6 | `FSATEMP` `FSAHUM` | **소수 2자리** (`'+22.35'` · `'47.67'`) | **소수 2자리** | Radionode 원값이 2자리다(실측).  ⛔ *"ENS식 1자리"* 유추는 폐기 — `ENS1`–`ENS7` 은 *"중계 그대로"* 라 출처가 다르다.  ~~OI-16~~ 종결 | 5.0 · 5.8 |
| 7 | `PRESCNX` / `OVRSCNX` | 그대로 (`0` / `48`) | ⭐ **`16` / `0`** | 채널 선두 16 은 **CCD 의 dark reference columns 를 읽은 값**이라 prescan 귀속이다(CU 협의 완료).  ⛔ 합 불변식은 어느 쪽이든 통과하지만 헤더로 구간을 셈하는 도구(science converter 와 같은 방식 — guide 소비자 gmon 은 지금 `gmon.conf` 구간을 쓴다)는 **자리로** 영상 좌표를 셈한다 | 9.1 · 9.4 · 10.3 |
| 8 | `CHMAP` comment | × (science 는 `CHMAP_*` 4장) | ⭐ **`[TBC]` 제거** | 5.0절에 *"comment 에 검토 표식을 싣지 않는다"* 를 규범으로 세웠다 — 값의 확정 여부는 문서(OI-21)가 든다 | 5.0 · 10.3 |
| 9 | 블록 `COMMENT` 2장 | **밑줄 추가** | **밑줄 추가** | `Exposure Information`(밑줄 47) · `Camera System House Keeping Data`(밑줄 35) 만 나머지 다섯과 달랐다.  규칙(78열까지)도 5.0절에 명문화 | 5.0 |
| 10 | `HTREN` `HTRSET` `HTROUT` `HTRFORCE` | 카드는 **그대로** | 카드는 **그대로** | ⭐ 바뀐 것은 **원천 배선**이다 — 넷 다 실기에서 실값이 실린다(설정 셋은 HK 바퀴의 `RCONFIG` 되읽기).  ~~OI-25~~ 종결.  ⛔ 하류가 *"구현 전이라 늘 `NC`"* 를 전제하면 안 된다 | 5.6.2 · 8장 |
| 11 | `HKUDATE` | 셈 규칙 **규범화** | 셈 규칙 **규범화** | **guide 유닛 실측 중 가장 오래된 표본시각**이고 `Radionode` 3장은 셈에서 뺀다.  ⛔ 그래서 이 카드로 `HEBOX`·`FSATEMP`·`FSAHUM` 의 나이를 판단하면 안 된다 | 5.6 · 10.4 |
| 12 | `DSAZ` `DSTELAZ` `DAZERR` | 출처 **redis** | 출처 **redis** | 돔 제어 프로그램이 실어 두는 값을 직접 읽는다(**D-021**).  키 TTL 이 지나면 `NC` — 옛 값을 이어 싣지 않는다.  ⭐ **읽는 시점이 다르다**: science 는 노출 개시 1회, guide 는 **프레임마다** | 5.7 · 5.7.3 · 10.1 |
| 13 | `DALTERR` | **불변** (`ICS calculation`) | **불변** | 고도차라 이 결정 밖이다 — ⛔ 방위값으로 덮지 않는다 | 5.7 · 5.7.3 |
| 14 | `DEWPRES` | sentinel 구간 **넓힘** | sentinel 구간 **넓힘** | 게이지 Off 뿐 아니라 **예열·재점등 대기** 구간도 `'9.99e-9'` 다(최악 약 2분).  같은 게이지라 양쪽이 동시에 sentinel.  ⚠️ 2026-09-14 결정으로 **끈 직후 한 바퀴는 예외**가 됐다 — §1 게이지 Off 행 | 5.6 · 10.4 |
| 15 | HK 온도 카드 전반 | **실측 보존** | **실측 보존** | 센서 한계 밖·미연결 값(≈`'-273.20'`)도 그대로 싣는다 — sentinel 은 장치가 값을 안 준 자리에만.  ⚠️ 하류는 **물리적으로 말이 안 되는 값**을 만날 수 있다 | 5.0 · 5.6 · 10.4 |
| 16 | (카드 아님) 노출 번호 | **규범 신설** | **규범 신설** | 저장되지 않은 프레임은 번호를 **소비하지 않는다**(**D-022**).  `STOP` 은 저장을 마치므로 먹고, 반쪽 pair 도 한쪽만 저장되면 소비다.  결번 허용 예외 넷 | 2.3-8 · 5.4.1 · 9.2 |

> ⚠️ **하류(LEECU)가 실제로 손댈 것은 하나다** — MEF Keywords 정의서의 `OBSTYPE`
> 설명(*"observation type"* 이 이제 프레임 종류가 아니다 — ⏳ 정의서 v1.1 에도 그대로다).
> `DAZERR` 는 **기록만**이다 — 2026-09-23 에 찾아보니 정의서(Dome 목록에 이름만)·converter
> (comment *"dome-telescope azimuth difference"*)·`mef_pipeline` 어디에도 *"ICS 파생값"*
> 이라는 서술이 없다.  그런 서술은 v1.12 이전의 raw 쪽 문서에만 있었고 v1.13 에서 고쳤다.
> 나머지도 **기록**이다.
>
> ⛔ **guide 는 MEF 경로가 없다** (소비자는 `gmon`) — 위 표의 guide 열 변경은 converter
> 에 닿지 않는다.  다만 **7번(`PRESCNX`)은 gmon 의 분할·좌표 셈이 따라야 한다.**

## 1.2 기존 C-항목 현황 — converter v2.5.0 재대조 (2026-09-23)

raw spec 6장은 *"LEECU 실행 목록은 통합 문서"* 라고 가리키는데, 규격 v1.2 7장(`archive/`, 동결본)에서
정의된 **기존 C-항목**은 지금까지 이 문서에 없었다 — 원장·규격·DECISION_LOG 가 번호로만 인용했다.
아래는 그 정의를 archive 에서 **인용만** 하고(문구는 고치지 않는다) 현행 상태를 converter **v2.5.0**
(D-023) 코드로 다시 맞춘 것이다.  C-7(완료) · C-8 · C-9 · C-10 · C-16 은 converter 밖(`ics_sim` ·
`cam_char`) 항목이라 뺐다.

⭐ **v2.5.0 에는 경고 층이 생겼다** — 멈추지 않는 이상(geometry 선언 불일치 · pair 동일 카드 불일치 ·
`CHMAP` 불일치 · 파일명 fallback · 포인팅 파싱 실패 · `Cn_*` 자리 수 불일치 · 문자열 절단)을 노출마다
`ConverterWarning` 으로 알린다.  그 가운데 geometry · pair · `CHMAP` 불일치는 PRIMARY `HISTORY`(종류마다 최대 8건)와
sidecar `.hdu_verify.txt` 에도 남고(`CHMAP` 은 PRIMARY `CHMAPOK=F` 로도 · `.summary.txt` 는 geometry·pair 건수만),
포인팅 파싱 실패는 `WCSOMIT=T` · `HISTORY` 두 줄 · sidecar 두 개의 WCS 항목으로 남는다 — 파일명 fallback · `Cn_*` 자리 수
불일치 · 문자열 절단은 경고로만 나가고 그 사실이 파일에 기록되지 않는다.  `main()` 이 이 경고를 `always` 로 걸어 두므로 **`python -W error` 로
돌리면 경고가 실패로 올라간다.**  카드 값 하드 실패는 §1 *"카드 값 하드 실패의 조건"* 행.

| C-항목 | 원 정의 (raw spec v1.2 7장 — `archive/`, 인용만) | converter v2.5.0 상태 | 남은 요청 |
| --- | --- | --- | --- |
| C-1 · C-2 · C-3 | NT 헤더 읽기 · `TELEMETRY` 2행을 `CTRLID`/`CTRLFW`/`BCKTEMP`/`READTIME`/`CTRLSTAT`/`CTRLERR` 로 · `VOLTINFO` 를 `VOLTN` + `VOLT<n>`… 로 | **대체됨** — 원천 카드가 raw 에서 폐지돼 C-17 · C-18 이 이어받았다 | — |
| C-4 | pair 일관성 검사 — `DATE-OBS`·`EXPTIME`·`RAWVER` 가 다르거나 두 `UNIQNAME` 의 날짜·번호 필드가 다르면 **변환 중단** | ◐ **부분** — `check_raw_pair()` 가 raw spec 5.9절 *"반드시 동일"* 카드 가운데 22장(`EXPID` · `DATE-OBS` · `EXPTIME` · `OBJECT` · `IMAGETYP` · `FILTER` · `RA` · `DEC` · `RADECSYS` · `EQUINOX` · `CTRL1ID`/`CTRL1SN` · `CTRL2ID`/`CTRL2SN` · `C1_*` · `C2_*` · `HKUDATE` · `CCDTEMP`)을 견주고 양쪽 `DETID` 가 같으면 알리지만, **멈추는 것은 `EXPID` 불일치 하나**다(둘 다 있을 때).  나머지는 경고 층 | `DATE-OBS`·`EXPTIME` 불일치를 멈춤으로 올릴지 — **LEECU 판단, §7-3** (`RAWVER`·`UNIQNAME` 은 raw 에서 폐지 — 해당 없음) |
| C-5 · C-13 | raw geometry 선언을 자기 상수와 대조, **불일치 시 중단** (C-13 은 `DETSIZE`·`COLGAP`·`ROWGAP`·`AMPPCD` 까지) | ◐ **반영 — 단 멈추지 않는다** (`check_raw_geometry()`, §1 C-5·C-13 행).  카드가 없으면 대조 없이 지나간다 | 멈춤으로 올릴지 — **LEECU 판단, §7-3** |
| C-6 | `DATE-OBS` 누락 시 현재 시각으로 조용히 대체하지 말고 **실패** | ✗ **미반영** — PRIMARY `DATE-OBS`·`MJD-OBS`·`JD`·`UT` 는 **변환 시각**, amp `UT` 는 빈 문자열이 된다.  경고도 없다.  `EXPTIME` 결측도 `0.0`(→ `DARKTIME` 0.0) | 요청 유지.  ⚠️ raw spec 5.0절이 두 카드를 **금지 열 장**에 넣고 비운 결함은 raw spec 7장 체크리스트 3·8번이 드러내게 했지만, converter 앞에 그 검사가 없으면 MEF 에서는 그럴듯한 값이 된다 |
| C-11 | `MODULE`/`CHANNEL` 을 raw 에서 채우고 `XTALKGROUP` 도 그 값으로 | ✅ **반영** (`CHMAP_*` 4장 — §1 C-11 행) | — |
| C-12 | amp `READDIR` 를 raw 에서 채운다(`amp<=8` 하드코딩 대체) | ⚠️ **코드 불변** — 커밋 제목·D-023 은 converter v2.5.0 이 다룬 항목으로 올렸다 (§1 C-12 행) | 닫을지 — **LEECU 판단, §7-4** |
| C-14 | `XTALKVER`/`REFVER`/`CATVER` 를 raw 가 아니라 calibration DB 에서 주입 | ✗ **미반영** — 여전히 raw 에서 읽고 없으면 `'UNMEASURED'` · `'N/A'` · `'N/A'` | L0 수록 여부 — **pipeline 판단, §7-5** (계층 규칙은 §4 `XTALKVER` · `REFVER` · `CATVER` 항목) |
| C-15 | `TELEMETRY` `TELSTAT` 을 양쪽 `CTRLSTAT` 에서 파생 | ✅ **다른 형태로 반영** — raw 에 `CTRLSTAT` 이 없어져서 `Cn_*` 파싱 상태(`OK`·`NC`·`UNKNOWN`·`PARTIAL`)에서 유도한다 | — |
| C-17 | `convert()` 가 NT 헤더의 메타데이터도 읽어야 한다 | ✅ **반영** — `amp_header()` 가 그 chip 이 든 파일의 헤더를 받는다.  PRIMARY 정체·HK·포인팅은 여전히 MK 에서 읽고, 그 근거인 5.9절 동일성을 `check_raw_pair()` 가 검사한다 | — |
| C-18 | `telemetry_rows()`·`volt_rows()` 가 헤더를 받지 않는다 | ✅ **반영** (§1 C-후보 `VOLTINFO`/`TELEMETRY` 행) | — |

## 2. raw ↔ MEF 키워드 이름 대응 (raw 개명 · 신설분)

raw 쪽 Detector/Amplifier 블록 확정(2026-08-21)으로 이름이 갈라진 것들이다 — 이름은 같은데 값·출처가 다른 카드(`PIXSCALE`·`PIXSIZE` ·
`DETID` · `ORIGIN` · `FILENAME` · `CAMVER`)도 함께 둔다.  ✅ **converter v2.5.0 은 이 대응으로 대조한다**(C-5·C-13) —
`check_raw_geometry()` 가 geometry 선언을 자기 상수와 견주고, 어긋나면 `ConverterWarning` 과 PRIMARY `HISTORY` 를 남긴 채
변환은 계속한다(카드가 없으면 대조 없이 지나간다 · `NAXIS1`/`NAXIS2` · `CCDXBIN`/`CCDYBIN` 도 대조한다).  converter v2.4.0 까지는
geometry 선언을 자기 상수와 견주지 않았고(`AMPNAX1`~`OVRSCNY` 여덟은 읽지도 않았다), 그래서 이 표는 C-5 대조를 붙일 때 쓸 대응이었다.  신설 카드 여럿도 이제 PRIMARY 로 간다.
**v2.5.0** 열이 2026-09-23 재대조 결과다 — *대조* = 선언을 상수와 견줌 · *중계* = raw 값을 그대로 MEF 로 · *상수* = raw 를 읽지 않고
converter 값을 씀 · *미독* = 읽지 않음.  행 번호는 판마다 밀리므로 함수 이름으로 적는다.  이 표 밖에서도 `DATASRC` · `FPAID` ·
`ICSBUILD` · `LEDFLASH` · `ENS1`–`ENS7` 이 v2.5.0 부터 PRIMARY 로 간다 (§1 기록 행 · §6-4).

| raw (신) | MEF / converter 쪽 | v2.5.0 | 비고 |
| --- | --- | --- | --- |
| `AMPNAX1` = 1200 | `RAWXTILE` | **대조** (`RAW_XTILE`) | 값 동일, 이름 상이 |
| `AMPNAX2` = 4700 | (없음) | **대조** (`RAW_NAXIS2 // 2`) | NAXIS2/NEND 타일 규약 값 |
| `IMAGEX` = 1152 | `AMPDATA` | **대조** (`AMP_DATA_COLS`) | |
| `IMAGEY` = 4616 | PRIMARY `TOPROWS` · `BOTROWS` · amp `NAXIS2` (상수 `ACTIVE_HALF_ROWS`) | **대조** (`ACTIVE_HALF_ROWS`) | ⚠️ v0.10 까지 MEF 쪽을 *"(카드 없음)"* 으로 적었으나 PRIMARY `TOPROWS`·`BOTROWS` 가 이 값을 싣는다 |
| `PRESCNX` = 0 (science) / **16** (guide) | `PRESCANX` | **대조** (`PRESCAN_X` = 0 — science) | 레거시 실측 27과 분리하려고 raw 쪽을 개명 — ✅ 삼자 모순은 **원장 v1.13 확인 요망 10 에서 종결**(키워드 변경 계승, 운영자 확정 2026-08-22).  ⚠️ **guide 는 값이 0 이 아니다** — 채널 선두 16 이 CCD 의 **dark reference columns** 를 읽은 값이라 `PRESCNX=16`·`OVRSCNX=0` 이다(raw spec v1.13 · 10.3절, 운영자 확정 2026-09-08).  guide 는 MEF 경로가 없어 converter 파급은 없지만 **gmon 쪽 분할·좌표 셈은 이 귀속을 따라야 한다** |
| `PRESCNY` = 0 | (없음) | **대조** (0) | |
| `OVRSCNX` = 48 | `OVERSCNX` | **대조** (`OVERSCAN_X`) | 레거시 실측 32와 분리하려고 raw 쪽을 개명 |
| `OVRSCNY` = 84 | (없음 — `MIDOVSCY`=168=2×84 관계, 분배는 OI-4) | **대조** (`MIDDLE_OVERSCAN_Y // 2`) | MEF `MIDOVSCY` 는 상수 168 이다 — `MIDOSC*` 카드는 미도입 (§5 OI-4) |
| `NAMPDET` = 16 | `AMPPCD`(정의서) | **대조** (16) | raw는 `NAMPS` · `AMPPCD` 폐지 (Header_and_Refs v1.6 8.1절) |
| `NAMPRAW` = 32 | (없음 — raw 파일 단위 개념) | **대조** (32) | |
| `PIXSCALE` · `PIXSIZE` (**같은 이름**) | MEF `PIXSCALE` · `PIXSIZE` = converter 상수 (`0.395` · `10.0`) | **대조**(허용오차 1e-6) · MEF 값은 **상수** | raw 선언이 바뀌어도 MEF 값은 따라가지 않는다 — 어긋나면 경고·`HISTORY` 가 남을 뿐이다.  `PIXSCALE` 하나가 seed WCS 의 `CD` 전체를 정한다(정의서 v1.1) |
| `CHMAP_LT/LB/RT/RB` | amp 헤더·AMPINFO `MODULE` · `CHANNEL` · `CTRUNIT` · `CCDPORT` · `CHANNAME` · `CHANNUM` · `IMGSEC` · `CHMAPSRC` (C-11) | **읽는다** — amp 정체를 유도하고 내장 `Detector_Ch_to_AmpID_Map_v1.1` 과 대조(PRIMARY `CHMAPOK` · `AMPIDMAP`) | 값 = CCD 출력 채널, **고정 4자 토큰 8개** — `<chip><A\|D><nn>`, 01–08=`A` · 09–16=`D` (raw spec v1.5, 2026-08-25 개정. **구 3자 `M16` 을 파싱하는 코드는 고쳐야 한다**) |
| `DETID` = 'MK'/'NT' | MEF `DETID` = converter 상수 **`'MKNT'`** (**같은 이름, 다른 값 — 정상**) | **교차검증만** — 파일명 접미사(`.MK`/`.NT`)와 견주고, pair 양쪽이 같으면 경고 | 레거시 계승, 값 재정의(pair).  ⚠️ v0.10 까지 MEF 쪽을 *"((TBD))"* 로 두었다 — v2.5.0 이 `'MKNT'` 로 정했다(정의서 v1.1) |
| `ORIGIN` (**같은 이름**) | MEF `ORIGIN` = converter 상수 `'KASI'` | **미독** (상수) | raw 는 **생성 사이트**, MEF 는 **파이프라인 산출처**라 값이 다른 것이 정상이다 (§1 `ORIGIN` 행) |
| `FILENAME` | MEF `FILENAME`(자체 생성) · AMPINFO `RAWFILE` · PRIMARY `MKFILE`/`NTFILE` | **미독** (디스크명만 사용) | converter는 raw `FILENAME` **카드**를 읽지 않는다 — 이름은 같아도 뜻이 다르다(raw 저장명 ↔ MEF 이름) |
| `EXPID` (v1.6 — 구 `ORIGNAME`) | PRIMARY `EXPID` | **중계** · pair 불일치 = **하드 실패** | **pair 양쪽 동일** — 짝 탐색 키로 쓸 수 있다 (§1 `EXPID` 행) |
| `UNIQNAME` (폐지) | ~~MEF `UNIQNAME`~~ (v2.5.0 폐지) | — | §1 C-신설 참조 |
| `DMPTEMP` (신설) | PRIMARY `DMPTEMP` | **중계** (문자열) | HK 재구성, §1 참조 |
| `WALLBRD` (신설) | PRIMARY `WALLBRD` | **중계** | 〃 |
| `HEBOX` (신설) | PRIMARY `HEBOX` | **중계** | 〃 — 나이는 `HKUDATE` 로 읽을 수 없다 (§1 HK 히터 행 ④) |
| `HKUDATE` · `HTREN` · `HTRSET` · `HTROUT` · `HTRFORCE` (신설, v1.10) | PRIMARY 같은 이름 | **중계** | §1 HK 히터 넷 행.  ⏳ 정의서 v1.1 §4.8 에 아직 없다 |
| `CCDTEMP` (의미 변경) | MEF `CCDTEMP` (L1 `CARRY_KEYS`) | **중계** · pair 동일 검사 | 평균 파생 → 대표 센서 실측, §1 참조 |
| `TCSTIME` (신설) | PRIMARY `TCSTIME` | **중계** | TCS 시각계 선언 — `TIMESYS`(ICS)와 분리 |
| `CTRL1CFG` / `CTRL2CFG` (신설) | PRIMARY `CTRL1CFG` / `CTRL2CFG` | **중계** (없으면 `'UNKNOWN'`) | 버전 문자열 6장을 귀속 (v1.9 3.3절).  ✅ MEF 목적지가 생겼다 — §6-4 ② 종결 |
| `CAMVER` (신설, v1.9) | MEF `CAMVER` = converter 상수 `'CEU-v2.1'` | **미독** (상수) | 카메라 시스템 버전 선언.  ⚠️ **MEF 에 같은 이름 카드가 이미 있고 그것이 상수다** — raw 가 범프해도 MEF 는 따라가지 않는다 (§1 C-신설 `CAMVER` 행).  ⚠️ **값이 올라가도 신호 계통은 그대로일 수 있다** — 듀어 RTD 의 배치·귀속 변경도 범프 사유이기 때문이다(raw spec 5.2절 `CAMVER` 행 — 범프 사유 전량 · 4.3절). 이 값을 캘리브레이션 게이팅의 세대 판별에 쓰는 쪽은 `CAMVER` 값↔구성 대장(raw spec OI-29)으로 HW 세대 변경과 RTD 재배치를 갈라 봐야 한다 |
| `RDMODE` (신설, v1.9) | PRIMARY `RDMODE` — MEF `READMODE`(`'64AMP'`, 상수) 와는 **별개** | **중계** (카드가 없으면 `'UNKNOWN'` · raw 결측값 `'NC'` 는 그대로 — v1.14) | raw 독출 속도 모드 선언. 구 `READMODE` 이름 충돌의 해소형(§3 참조) |
| `C1_TEMP`·`C1_VOLT`·`C1_CURR` / `C2_*` (신설, v1.9) | PRIMARY 같은 이름(문자열 그대로) + `VOLTINFO` / `TELEMETRY` (C-18) | **중계 + 해석** | 컨트롤러별 텔레메트리(구 `BCKTEMP` 확장).  자리 순서는 raw spec 5.6.1절 · PRIMARY `RAILREF` (§1 C-후보 행) |

## 3. ICD v4.3 이후 개정 후보

> 제목의 판은 v0.10 까지 *"ICD v4.1"* 이었다 — ICD 가 v4.2(D-017) · v4.3(D-023, 2026-09-23)으로 올라서 아래
> 항목마다 v4.3 반영 여부를 달았다.

- **raw spec 인용 판 갱신 (raw spec v1.14 발행)**: ICD v4.3 은 raw spec 을 **파일 경로** `raw_fits_spec/KMT_CEU_Raw_FITS_Specification_v1.13.md` 로 세 곳(41행 — 5장 · 83행 — 2.2·2.3절 · 297행 — 5.6·5.6.1절), **판 표기**로 네 곳(76행 — 2.2절 · 160·230행 — 4.3절 · 300행 — 4.3절·OI-3·OI-17 ③)에서 인용한다.  v1.14 발행으로 v1.13 파일은 `archive/` 로 갔으니 **`…Specification_v1.14.md` 로 갱신 요청**.  ⭐ v1.14 는 전반 재검토로 2.2·2.3절과 5장 여러 절(5.0·5.4·5.5·5.6·5.6.1 등)을 고쳤지만(raw spec 12장 v1.14 행), ICD 가 인용한 사실 가운데 ICD 문면을 바꿔야 하는 것은 §2.1 교차검증 문장(다음 항목 — v1.14 2.2절도 같은 조건을 적었다)뿐이다 — 4.3절 · OI-3 · OI-17 ③ 은 그대로이고, 2.2절 관측일 경계는 ICD 가 인용만 한다(아래 관측일 경계 항목).  나머지는 판 표기만 고치면 된다.  PRIMARY `RAILREF='RAWSPEC-v1.13-5.6.1'`(converter · 정의서 v1.1)은 v1.14 5.6.1절의 **자리 순서**가 v1.13 과 같으므로 **참**이다(바뀐 것은 자릿수 주 · 예시 바이트꼴 · 표기 체계 주의 원장 인용뿐이다) — 자리 순서가 바뀔 때 올리면 된다.
- **§2.1 교차검증 문장의 조건**: 81행 *"the converter (v2.5.0) derives the output MEF prefix from the filename site code and raises an error on mismatch"* 는 조건 없는 문장인데, 코드에서는 **기본 출력 이름 경로에서만** 돈다(`-o` 없음 · 파일명이 D-011 문법에 맞음 · `OBSERVAT` 가 네 값 안 — §1 *"카드 값 하드 실패의 조건"* 행).  converter 쪽 요청이 반영될 때까지 문장에 조건을 달 것.  §11 예시(`-o kmtk.20260915.000034.ceu.l0amp.mef.fits`)는 바로 그 검사를 건너뛰는 경로다 — 예제 스크립트처럼 `-o` 없이 쓰거나 그 사실을 적을 것.  ⭐ `EXPID` pair 불일치도 이제 하드 실패다 — §2.1 이나 §11 에 함께 적을 것.
- ✅ **§2.1 사이트 코드 정규화 충돌 — v4.3 에서 정리됐다**: v4.2 의 *"normalized to `KMTK` with a warning on the acquisition side (DECISION_LOG D-017)"* 는 D-020 · raw spec 2.2절(*"그 넷 밖의 값은 기동을 거부한다"*)과 어긋나 있었는데, v4.3 §2.1 이 D-020 문단을 넣어 정리했다(76행).
- **관측일 경계 (KASI +9 h)**: ICD v4.3 §2.1 은 `<YYYYMMDD>` 를 *"observing-night date"* 로만 적고 경계는 raw spec 2.2절·D-017 을 인용하므로 **문면은 고칠 것이 없다** — 인용한 D-017 항목 4 가 개정됐다는 알림만 (§1 기록 행).
- **13장 개정 이력의 절 번호**: v4.3 행이 *"Section 4 gains the note that "no chip-dependent flip" …"* 이라고 적었지만, 그 주석(*"Note added in v4.3 (D-023 decision 4)"*)은 §6(*"Why L0 must preserve 64 amplifier images"*) 안에 있고 §4 는 v4.2 와 같다.  이력의 절 번호를 §6 으로 고칠 것 — 바뀐 절을 이력으로 찾는 쪽(국문본 번역 등)이 §6 을 놓친다.
- **`mef_fits_spec/README.md` 의 낡은 인용 둘**: 57행이 아직 `../raw_fits_spec/KMT_CEU_Raw_FITS_Pair_Spec_v1.2.md`(2026-08-22 개명 이후 없는 경로)를 v4.0 → v4.1 근거로 가리키고, 75행은 `raw_fits_spec/__reference/` 의 사본을 *"…Main_Keywords_Final_v1.1.md"* 라고 부른다 — 실제 파일은 `KMT_CEU_MEF_FITS_Main_Keywords_Final_v1.0.md` 다(`mef_fits_spec/archive/` 의 v1.0 과 바이트 동일).  LEECU 소관이라 보고만 한다.
- **`OVERSCNY` 이름 위험의 문서화 (v1.10 지시)**: 레거시 `OVERSCNY` 는 가장자리 Y overscan(값 0), 신규는 **영상 중앙**(168행) — 이름을 물려주면 "위쪽 N행 자르기" 도구가 active 픽셀을 지운다. raw 는 **`OVRSCNY`**(amp 당 84, frame-center side)로 개명해 잘림을 방지했다. **ICD/정의서에 이 위험 사유와 개명 사실을 기록**해 하류 도구 작성자가 레거시 이름을 재사용하지 않게 할 것 (MEF 는 `MIDOVSCY` 계열 유지).  ⏳ v4.3 에도 아직 없다.
- ~~**§12 (open items)**: raw 텔레메트리 집합의 위임 대상이 구 규격 5장 → 재작성판으로 바뀐다. 참조 갱신.~~ → ✅ **v4.3 §12 에서 반영** — raw spec 5.6·5.6.1절을 가리킨다(판 표기만 남았다 — 첫 항목).
- ~~**`READMODE` 값 충돌**: ICD/정의서는 `READMODE='64AMP'`(구조 선언), raw 초안은 `'FAST'`(독출 속도 모드)로 쓰려 했다 — 같은 이름, 다른 뜻. 이름 분리 필요~~ → **해소(v1.9)**: raw 쪽이 **`RDMODE`** 로 개명 도입되어 이름이 갈라졌다 — MEF `READMODE='64AMP'` 는 그대로, **ICD 개정 항목 없음**.
- ~~**AMPINFO의 상류 공급원 명시**: "authoritative 64-row map"의 배선 열(MODULE/CHANNEL)이 converter 추정식이 아니라 **raw `CHMAP_*` + 재작성판의 amp 전수 표**에서 온다는 것을 명시.~~ → ✅ **v4.3 §8 에서 반영** — `MODULE`·`CHANNEL` 을 raw `CHMAP_*` 에서 읽고 `Detector_Ch_to_AmpID_Map_v1.1.txt` 와 대조한다고 적었다(C-11).
- **overscan 좌우 패턴 — 종결(2026-08-22)**: 신규는 **`RRRRLLLL`(4:4) 확정**이다(실제 획득 자료 육안 확인, raw spec 4.1절). 레거시 MEF `AMPSEC` 의 M/T=5:3 · K/N=3:5 는 레거시 계통의 관찰이므로 신규 L0 에 적용하지 말 것 — ICD·정의서가 레거시 패턴을 전제하고 있으면 갱신 대상이다. geometry 가 바뀌면 **raw 쪽은 `CAMVER`(HW)/`CTRLxCFG`(설정) 범프 · MEF 쪽은 `GEOMVER` 동반 범프** — `RAWVER` 는 미도입 확정이다(Header_and_Refs v1.13 확인 요망 11: 규격/구성 버전은 `CAMVER`·`CTRLxCFG`·`DETID`·`CHMAP_*` 조합으로 파악).
- ✅ **파일명 `<SITE>` 넷째 코드 개정 (D-017, 2026-08-25) — 반영 완료** (ICD v4.2 §2.1 · converter v2.4.0 — §1 C-재개 행). `KMTT`(TESTBED) → **`KMTK`(KASI)**, L0 MEF prefix `kmtt`→`kmtk`. **형식(필드 폭·구분자·6자리 zero-padding)은 불변**이고 바뀐 것은 코드 하나다.  ⚠️ v0.10 까지 이 자리에 적었던 *"converter 정규식의 넷째 대안을 바꾸지 않으면 KASI 자료가 `find_pair()` 에 걸리지 않는다"* 는 **틀린 서술이었다** — 정규식은 출력 이름을 짓는 `default_output_name()` 에만 있고, 짝을 찾는 `find_pair()` 는 접두어와 무관하게 접미사만 바꿔 끼운다(converter docstring *"find_pair() is prefix-agnostic"*).  정규식이 안 맞으면 출력 이름만 대체 규칙으로 지어진다(v2.5.0 부터는 경고와 함께).
- **번호 공간 확대 (D-018, 2026-08-25)**: `000000`–`099999` → **`000000`–`999999`**. 정규식이 `\d{6}` 이라 **converter·ICD 형식 영향은 없다** — 다만 "앞자리가 항상 `0`" 을 전제한 도구가 있다면 그건 깨진다.
- 충돌 번호 증가 시에도 파일명 형식은 같고 번호만 다르다.

## 4. MEF Keywords 정의서 v1.1 이후 개정 후보

> 제목의 판은 v0.10 까지 *"v1.0"* 이었다 — 정의서가 v1.1(D-023, 2026-09-23)로 올라서 아래 항목마다 v1.1 반영
> 여부를 달았다.  ⭐ 새 항목 다섯은 converter v2.5.0 과 정의서 v1.1 의 어긋남이다.

- **HK 중계 카드 등재**: converter v2.5.0 이 PRIMARY 에 싣는 `HKUDATE` · `HTREN`/`HTRSET`/`HTROUT`/`HTRFORCE` · `DMPTEMP` · `WALLBRD` · `HEBOX` · `ENS1`–`ENS7` · `C1_`/`C2_` `TEMP`/`VOLT`/`CURR`(문자열) 가 §4.8 표에 없다.  등재하면서 sentinel 어휘(`'-999.99'` 온도·습도 · `'9.99e-9'` `DEWPRES` · `'NC'` 상태 낱말·전압·시각)와 **실측 보존** 성질(§1 HK 온도 행 ⑤ — 한계 밖·미연결 값도 그대로 온다)을 함께 적을 것.
- **`EQUINOX` 두 행** — raw v1.14 가 실수이고 결측이 `-999.0` 이라는 것을 적고 *"raw는 문자열 `'2000.000'`으로 넘기는데"* 는 v1.13 이전의 말로 고칠 것 (§1 기록 행 `EQUINOX` 실수형).
- **`UT` 행** — *"폐지된 `TSHOPEN`은 시각 부분을 비운 채 넘어온다"* 는 사실이 아니다(v2.4.0 도 `TSHOPEN` 이 없으면 `DATE-OBS` 전체를 실었다 — §1 C-신설 `UT` 행).
- **`ORIGIN` 행** — *"From raw"* 로 적혀 있지만 converter v2.5.0 은 상수 `'KASI'` 를 쓴다 (§1 `ORIGIN` 행).
- **`CAMVER` 행** — `Required` 상수 `CEU-v2.1` 이다.  raw 의 범프를 따라가도록 *"From raw"*(raw 에 없을 때만 기본값)로 고칠 것 (§1 C-신설 `CAMVER` 행).
- **`XTALKVER` · `REFVER` · `CATVER` 의 계층 규칙 (운영자 확정 2026-08-22)**: 이 셋의 정본은 **pipeline calibration DB** 다(C-14) — HW·성능 변화 없이도 pipeline setup 에서 바뀔 수 있는 값이라 raw 는 싣지 않는다. **전처리 전 MEF(L0)에 넣을지는 pipeline 팀 판단**이고, **전처리 후 산출물(L1)에는 필수**다 — 보정에 실제 적용한 버전이므로. 정의서/ICD 에 이 계층을 명시할 것.  L0 에 넣을지의 판단은 **§7-5**.
- ~~`UNIQNAME` 항목: 공급원 변경 또는 폐지(§1 C-신설과 연동).~~ → ✅ **v1.1 에서 `Retired` 로 반영** (정체는 `FILENAME` + `EXPID`).
- `NAMPS`=64 · `AMPPCD`=16: raw 쪽 폐지(v1.6 8.1)와의 관계 명시 — MEF 유지 여부는 **LEECU 판단, §7-6**.
- ⚠️ **Thermal/dewar 행에서 `AIR_IN`·`AIR_OUT`·`GLYC_IN`·`GLYC_OUT` 4장의 거취 (2026-08-25)**: raw spec v1.5 가 이 넷을 **폐지**했다(5.6절 18장→14장, 5.10절 등재). 정의서 v1.0 은 아직 넷을 싣고 converter 도 pass-through 한다. 이로써 `standalone RTD readout unit` 계통이 raw 헤더에서 완전히 비었다(공급은 `ICG RTD`·`Radionode` 둘만 남는다).  ⏳ **v1.1 §4.8 Thermal/dewar 행과 converter v2.5.0 에서도 그대로다**.  MEF 에서 폐지할지, 다른 공급원을 둘지는 **LEECU 판단, §7-2**.
- **사이트별 상수표 신설 (D-017 항목 6, raw spec 5.3.1절)**: `TELESCOP` = CTIO `#1` · SSO `#3` · SAAO `#2` · KASI `#0` / `FPAID` = CTIO `FPA#2` · SSO `FPA#1` · SAAO `FPA#3` · KASI `FPA#0`. ⚠️ **망원경 번호와 FPA 번호는 관측소 셋 모두 어긋난다** — MEF 쪽에서 둘을 맞추는 파생을 넣으면 검출기 귀속이 틀어진다.  ⏳ v1.1 에도 표가 없다(`TELESCOP`·`FPAID` 모두 *"From raw"* 한 줄).
- **`OBSTYPE` 정의 갱신 (운영자 확정 2026-09-09)**: raw `OBSTYPE` 은 프레임의 종류가 아니라 **관측 계통**(`SCIENCE`/`GUIDE`)이다 — 정의서 v1.0 의 *"observation type"* 설명을 갱신하고, `IMAGETYP` 의 동의어로 읽는 하류 코드가 없는지 확인할 것.  ⏳ **v1.1 에도 *"observation type"* 그대로다** — v0.10 의 요청이 반영되지 않았다.
- (기록) 레거시 MEF의 `AMPNAME2`('im16')가 배선 identity를 헤더에 실은 선례 — `CHMAP_*` 채택의 계보.

## 5. 미결(OI-*)과의 연동

| OI | 이 검토와의 접점 |
| --- | --- |
| OI-3 (`ROWORDR`/`RDDIR*`) | 포장 규범 조항 이관 후 flat/star 시험은 "사실 확인"이 아니라 **준수 검증**이 된다 |
| OI-4 (중앙 168행 분배) | raw `OVRSCNY`=84는 타일 규약 값이다. 물리 분배는 **실측 후 규격에 기록한다** — `MIDOSCT`/`MIDOSCB` 카드는 **미도입**(원장 7장 · raw spec 5.10절, v1.9).  ⚠️ v0.10 까지 이 자리의 *"실측 후 `MIDOSCT`/`MIDOSCB` 로"* 는 v1.9 에서 뒤집힌 옛 지시였다 — converter 는 이 카드를 읽을 준비를 하지 않아도 된다(v2.5.0 은 MEF `MIDOVSCY`=168 을 상수로 쓰고 raw `OVRSCNY` 를 84 와 대조할 뿐이다) |
| ~~OI-17 잔여 ①·②~~ | **종결 (2026-08-25)** — 운영자가 **채널 번호 = OS 번호**를 확정(잔여 ②)했고, 그로써 `채널 09–16 = OS9–16 = 위 half = 섹션 D` 가 e2v 데이터시트까지 이어져 `IMGSEC` 의 `B-BOT` 16행이 **`D-BOT`** 으로 정정됐다(잔여 ①). 기계 정본이 `Detector_Ch_to_AmpID_Map_v1.1.txt` 로 판올림됐다 — **구 v1.0(3자 토큰·`B-BOT`)을 읽는 도구는 고쳐야 한다.** 잔여는 ③(K·N 조 180° 회전 장착 확인)뿐 |
| OI-13 (셔터 반영 지연) | 재질의 지연이 **3초 → 1초**로 바뀌었다(운영자 2026-08-25, raw spec 5.7.1절). `AUXQDATE` 가 `DATE-OBS` 뒤로 가는 경로의 문턱도 `EXPTIME > 1 s` 로 함께 내려간다 |
| ~~(신규 제안)~~ | ~~X overscan 패턴 4:4 vs 5:3 검증~~ — **종결(2026-08-22)**: 실제 획득 자료 육안 확인으로 `RRRRLLLL`(4:4) 확정 (raw spec 4.1절) |

## 6. MEF/converter 쪽 미결 안건 (v0.5 신설 — 구 검토 문서에서 이관)

구 검토 문서(raw ↔ MEF 키워드 대응표)의 팀원 검토 요청 항목 중 Header_and_Refs 사이클이 흡수하지 않은 **MEF/converter 쪽 안건 4건**이다. 이 이관으로 그 문서의 살아있는 내용은 전부 흡수됐고 문서 자체는 폐기됐다(운영자 재가 2026-08-22) — 판정 준거는 Header_and_Refs **0장**으로 편입됐다.

1. **`NCTRL` 정의** — 미결.  본문은 **§7-7** 로 옮겼다 (LEECU 판단).
2. **`CTRLID` 개칭 검토** — 미결.  본문은 **§7-8** 로 옮겼다 (LEECU 판단).
3. **`SATURAT` ↔ `SATLEVEL` 통일** — 미결.  본문은 **§7-9** 로 옮겼다 (LEECU 판단).
4. ~~**`DATASRC` · 설정 포인터의 MEF 목적지**~~ — ✅ **종결 (converter v2.5.0 · 정의서 v1.1)**: ① `DATASRC`(**시뮬 프레임이 실측으로 오인되는 것을 막는 유일한 카드**)와 ② `CTRL1CFG`/`CTRL2CFG`(구 `ACFFILE` 이 폐지되고 대체된 설정 provenance 포인터, v1.9)를 둘 다 PRIMARY 로 중계한다(없으면 `'UNKNOWN'`).  converter v2.4.0 까지는 두 카드 다 L0 MEF 에 자리가 없었다.

> 나머지도 흡수 완료다 — **판정 준거·구간 산정**은 Header_and_Refs **0장**, **카드 전수 대응표**는 같은 문서 각 장의 `Use in MEF` 열(구 `MEF 목적지`), **MEF 표 HDU 컬럼**은 원래부터 MEF 규격(현행 ICD v4.3 §8 · Main_Keywords v1.1) 소관이다. 재작성판 V1 의 amp 전수 표의 **기계 정본은 이 폴더 루트의 [`Detector_Ch_to_AmpID_Map_v1.1.txt`](Detector_Ch_to_AmpID_Map_v1.1.txt)** 다(구 v1.0 은 v1.7 에서 삭제 — git 이력).  converter v2.5.0 은 이 표를 소스에 박아(`AMPID_MAP_VERSION='Detector_Ch_to_AmpID_Map_v1.1'`) raw `CHMAP_*` 과 대조하고 PRIMARY `AMPIDMAP` · `CHMAPOK` 에 결과를 남긴다.

## 7. MEF converter 및 PIPELINE 판단 필요 항목 (v1.0 신설)

Part 1 의 다른 절은 대부분 **고칠 것**(converter · ICD · 정의서의 동작·문면 변경 요청)이다.  이 절에는 그와 달리
raw 쪽이 규범으로 정하지 않고 **MEF 쪽(converter · `mef_pipeline` · 아카이브)이 골라야 하는 것**만 모았다 — raw
규격은 결정이 나면 그 결정을 가리키기만 한다.  §1 · §1.2 · §4 · §6 에 흩어져 있던 판단 요청을 여기로 옮겼고,
원래 자리에는 이 절을 가리키는 포인터만 남겼다.  7-1 ~ 7-4 는 통합 v1.0 에 새로 올린 항목이고(7-2 는 §1 HK 온도 행 ④ · §4 의 `AIR_*` 거취 판단을 흡수했고, 7-3 · 7-4 는
converter v2.5.0 재대조에서 나왔다), 7-5 ~ 7-9 는 §4 · §6 에서 옮겨 왔다.  7-1 은 DECISION_LOG **D-001** 과 맞물린다.

| # | 판단 항목 | 사실 · 정할 것 | 판단 주체 · 원래 자리 |
| --- | --- | --- | --- |
| 7-1 | **반쪽 pair 의 성한 절반 — 보관 규칙** (2026-09-23) | raw spec 2.1절은 한쪽 실패로 성한 쪽까지 버리면 **되돌릴 수 없는 관측 손실**이라며 성공한 쪽을 저장하게 했고, 짝 없는 노출은 하류가 건너뛴다(§1 반쪽 pair 행).  그런데 DECISION_LOG **D-001** 은 **64 amp L0 MEF 를 primary raw archive** 로 두고(영향 *"`69 = PRIMARY + 64 IMAGE + 4 BINTABLE`"*), converter v2.5.0 `convert()` 는 MK·NT 를 둘 다 memmap 하고 모양을 검사해야 돈다 — 반쪽 L0 를 만드는 경로가 없다.  그래서 **성한 절반(chip 둘 · 32 amp)은 1차 아카이브 산출물에 들어갈 길이 없고 raw 로만 남는다.**  그 보관 경로는 규격·이 문서 어디에도 정해져 있지 않다.  **정할 것** — 선택지 예: **(a) raw 반쪽을 장기 보존 대상으로 명시** — L0 가 없는 관측 자료가 생기므로 D-001 의 *"64개 amplifier image extension을 가진 L0 MEF를 primary raw archive로 둔다"* 에 예외가 붙는다 **(b) 32 amp 반쪽 L0 를 허용**하고 빈 32 amp 를 어떻게 표시할지 정의 — D-001 영향의 69 HDU 구조를 지키더라도 L0 는 64 amp 가 다 찬다는 전제가 바뀌고, L1 `SCI_M`·`SCI_K`·`SCI_N`·`SCI_T` 가운데 둘이 비는 경우를 정해야 한다 | LEECU (converter · 아카이브) — **raw 쪽 규범 결정이 아니다**.  결정이 나면 DECISION_LOG 에 새 D-번호를 세우거나 D-001 에 개정 배너를 단다.  원래 자리: §1 반쪽 pair 행 |
| 7-2 | **raw 가 공급하지 않는 MEF 카드의 처분** (2026-09-23) | raw 가 싣지 않기로 **판정된** 카드인데 converter v2.5.0 이 raw 에서 읽으려다 기본값을 박는 자리가 남아 있다 — 버전 문자열 `CTRLVER`·`TIMVER`·`BIASVER`·`CLKVER`(`'ARCHON-v1.0'`·`'TIM-v1.0'`·`'BIAS-v1.0'`·`'CLK-v1.0'` — 진짜 판처럼 보인다.  raw 에서는 `CTRL1CFG`/`CTRL2CFG` 로 귀속됐다) · `CTRL1FW`/`CTRL2FW`(`'UNKNOWN'`) · `FSADEW`·`FSAALRM` · `CHKIMG`·`CHKIMG_C` · `TSHOPEN`·`TSHSHUT` · `CHSTAT` · `AIR_IN`·`AIR_OUT`·`GLYC_IN`·`GLYC_OUT`(빈 값 — raw spec v1.5 가 폐지해 standalone RTD 계통이 raw 헤더에서 비었는데 정의서 v1.1 §4.8 Thermal/dewar 행에는 넷이 남아 있다) · TELEMETRY `FWVERSION`/`READTIME`/`ERRORFLAG`(raw 를 읽지 않는 상수 sentinel `'UNKNOWN'` · `-1.0` · `-1` — ICD v4.3 §9 가 의도로 적었다).  ⚠️ **인접 결함**: 한 MEF 안에서 `BIASVER`/`CLKVER` 가 **값이 둘**이다 — PRIMARY 는 `'BIAS-v1.0'`/`'CLK-v1.0'`, `VOLTINFO` 헤더는 `'UNKNOWN'` (정의서 v1.1 도 두 자리를 따로 적는다).  원장 3.3절은 이 *"그럴듯한 provenance"* 위험을 경고만 하고 조치 항목이 없었다.  **정할 것**: 카드마다 ① MEF 에서 폐지 ② `'UNKNOWN'` 같은 명시적 미상 표기 ③ 다른 공급원(`CTRL1CFG`/`CTRL2CFG` · calibration DB — `XTALKVER`·`REFVER`·`CATVER` 는 7-5) 가운데 하나.  `BIASVER`/`CLKVER` 는 적어도 한 MEF 안에서 값이 하나가 되게 할 것.  ⚠️ `TSHOPEN` 의 공급원으로 `DATE-OBS` 를 쓰면 셔터 없는 노출에서 뜻이 어긋난다 (§1 `DATE-OBS` 기록 행) | LEECU (converter · 정의서).  원래 자리: §1 포인터 행(→ §7-2) · §1 HK 온도 행 ④ · §4 Thermal/dewar 항목 |
| 7-3 | **멈춤 기준 — 경고 층과 하드 실패의 경계** (C-4 · C-5 · C-13) | converter v2.5.0 이 카드 값으로 멈추는 것은 둘뿐이다 (§1 *"카드 값 하드 실패의 조건"* 행).  pair 동일 카드의 나머지 불일치(C-4 — v1.2 는 `DATE-OBS`·`EXPTIME` 이 다르면 **중단**하라고 했다)와 geometry 선언 불일치(C-5 · C-13 — v1.2 는 **중단**)는 `ConverterWarning` · PRIMARY `HISTORY` · sidecar 로만 남는 경고 층이다 (§1.2 머리).  **정할 것**: ① `DATE-OBS`·`EXPTIME` pair 불일치를 멈춤으로 올릴지 ② geometry 선언 불일치를 멈춤으로 올릴지 ③ 경고 층으로 둔다면 밤 단위 배치를 `python -W error` 로 돌릴지 — 그러면 경고가 전부 실패로 올라간다.  C-6(`DATE-OBS` 결측 시 실패)은 판단이 아니라 요청이다 (§1.2) | LEECU (converter · 배치).  원래 자리: §1.2 C-4 · C-5·C-13 행 |
| 7-4 | **C-12 `READDIR` 의 종결 여부** | converter v2.5.0 커밋 제목·D-023 은 C-12 를 v2.5.0 이 다룬 항목으로 올렸지만 amp `READDIR` 는 여전히 `amp<=8` 로 가르는 placeholder 다 — 바뀐 것은 *"독출 방향이 저장 순서에 닿지 않는다"*(raw spec 4.3절)는 논거뿐이다 (§1 C-12 행).  **정할 것**: C-12 를 *"`READDIR` 은 저장 순서에 닿지 않으므로 placeholder 로 충분하다"* 로 닫는 것인지 — 그렇다면 그 판단을 ICD §12 에 적고, 아니면 C-12 를 열린 항목으로 되돌린다.  OI-3(실기 확인)은 어느 쪽이든 유지 | LEECU (converter · ICD).  원래 자리: §1 C-12 행 · §1.2 C-12 행 |
| 7-5 | **`XTALKVER` · `REFVER` · `CATVER` 의 L0 수록** (C-14) | 셋의 정본은 **pipeline calibration DB** 이고 raw 는 싣지 않으며, **L1(전처리 후)에는 필수**다 (운영자 확정 2026-08-22 · 계층 규칙은 §4).  converter v2.5.0 은 여전히 raw 에서 읽고 없으면 `'UNMEASURED'` · `'N/A'` · `'N/A'` 를 싣는다 (C-14 미반영 — §1.2).  **정할 것**: 전처리 전 MEF(L0)에 넣을지 — 넣는다면 calibration DB 에서 주입하고(C-14), 넣지 않는다면 L0 의 세 카드를 어떻게 둘지(7-2 의 ① 폐지 · ② 명시적 미상 표기) | PIPELINE (`mef_pipeline` · calibration DB) — 계층 규칙이 *"L0 수록은 pipeline 팀 판단"* 으로 정했다.  원래 자리: §1.2 C-14 행 · §4 계층 규칙 항목 |
| 7-6 | **MEF `NAMPS` · `AMPPCD` 유지 여부** | raw 는 `NAMPS`·`AMPPCD` 를 폐지하고 `NAMPDET`=16 · `NAMPRAW`=32 를 둔다 (Header_and_Refs v1.6 8.1절).  MEF 는 converter v2.5.0 이 `NAMPS`=64 · `AMPPCD`=16 을 상수로 싣고 정의서 v1.1 도 둘 다 `Required` 다 — MEF 는 카메라 전체 관점이라 유지가 자연스러울 수 있다.  **정할 것**: MEF 에서 유지할지, 유지한다면 raw `NAMPDET`·`NAMPRAW` 와의 관계를 정의서에 적을 것 | LEECU (정의서).  원래 자리: §4 |
| 7-7 | **`NCTRL` 정의** | converter 가 `2` 를 하드코딩한다(PRIMARY `primary_cards()` · `TELEMETRY` 헤더 — v2.5.0 도 그대로).  **과학 2대만 센 값**인데 Archon 은 3대(과학 2 + 가이드 1)다.  정의서(v1.0 부터 *"science Archon controller 수"*)와 ICD v4.3 §5·§9(*"One row per science controller (`NCTRL` rows)"* · *"`NCTRL` (2 in the science product)"*)는 이미 과학 2대로 적는다.  **정할 것**: 이 정의로 닫을지 — 가이드까지 센 전체 수가 필요한 하류가 없으면 종결이다 | LEECU (ICD · converter).  원래 자리: §6-1 |
| 7-8 | **`CTRLID` 개칭** | MEF amp `CTRLID` 와 `AMPINFO`·`TELEMETRY` 의 `CTRLID` 열은 색인 정수(`1`/`2`)인데 raw/MEF PRIMARY 의 `CTRL1ID`(식별자 **문자열** `'KMTA-SCI-101'`)와 이름이 너무 닮았다 (v2.5.0 도 그대로다).  **정할 것**: `CTRLIDX` 등으로 개칭할지 | LEECU (ICD · 정의서 · converter).  원래 자리: §6-2 |
| 7-9 | **`SATURAT` ↔ `SATLEVEL` 통일** | converter **내부에서** 이름이 갈린다 — amp 헤더는 `SATURAT`, `AMPINFO` 열은 `SATLEVEL` 이다 (v2.5.0 도 그대로다).  **정할 것**: 어느 쪽으로 통일할지 | LEECU (ICD · 정의서 · converter).  원래 자리: §6-3 |

---

# Part 2 — raw 파일 번호 · 정체성 · 충돌 처리 (파급 요약)

> **정본 이동 완료**: 설계 전문은 **raw spec 2.3절**([`KMT_CEU_Raw_FITS_Specification_v1.14.md`](KMT_CEU_Raw_FITS_Specification_v1.14.md))과 DECISION_LOG **D-016**(Accepted, 2026-08-22)이다. 이 Part 는 MEF/구현 쪽 파급만 남긴다 — 골자: 충돌 시 노출 번호 증가(공간 **000000–999999**, 선검사, 상한 **1000000회** — **D-018**, 2026-08-25 로 구 `099999`·100000회를 대체) · `FILENAME`(유일 키) + **`EXPID`**(`FILENAME` 의 `DETID` 필드를 뗀 값과 불일치 = 충돌 신호 — v1.6 · **D-019** 로 구 `ORIGNAME` 을 대체) · `UNIQNAME`/`NAMECLSH`/`clash/`/`PAIRFILE`/`CTRLTAG` 폐지 · **저장되지 않은 프레임은 번호를 소비하지 않는다**(raw spec 2.3절 8항, 운영자 확정 2026-09-07 — 아카이브 번호는 연속이 원칙이고 결번은 예외 넷뿐이다).

> ⚠️ **아카이브·색인 도구에 주는 뜻** — 노출 번호의 **구멍은 정상 상태가 아니다**(예외 넷: 취득 SW 내부 오류 · 번호 공간 고갈 · 번호 기록 파일 읽기 실패 · 종료 저장 상한 초과). 그러나 **연속성을 무결성 판정의 근거로 삼지는 않는다** — 구멍의 뜻은 그때의 ICS 로그가 정하고, 파일 쪽 유일 키는 여전히 `FILENAME` 이다.

## 1. 하류 도구 요구사항

- 충돌 필터는 **raw 헤더 층**(아카이브 색인 · DTS · QL)에서 **`FILENAME` 의 `DETID` 필드(`.MK`/`.NT`)를 뗀 값 ≠ `EXPID`** 로 돈다 (v1.6 — 구 `FILENAME ≠ ORIGNAME`). 재저장 유령 중복은 fail-open — 이 필터가 거른다는 전제가 요구사항이다.
- 아카이브 근거는 **`FILENAME`(+`EXPID`)**. pair 쪽 식별은 `FILENAME` 의 `DETID` 필드 `.MK`↔`.NT` 치환 — **또는 `EXPID` 가 양쪽 같으므로 그 값으로 묶어도 된다** (v1.6).
- ~~MEF 층 필터가 필요해지면 converter 변경점에 `EXPID` pass-through 를 추가한다 (Part 1 §1).~~ → ✅ converter v2.5.0 이 `EXPID` 를 PRIMARY 로 싣는다 — MEF 층 필터가 필요해지면 그 카드로 돌 수 있다 (Part 1 §1).

## 2. MEF / converter 연동

✅ **converter v2.5.0 이 MEF `UNIQNAME` 을 폐지했다**(대안 (c) — 정체는 `FILENAME` + `EXPID`, D-016·D-019 · 정의서 v1.1 `Retired`).  v2.4.0 까지는 raw `UNIQNAME` 을 읽어 MEF `UNIQNAME` 으로 옮겼고, raw 폐지 뒤에는 그 값이 **오류 없이 빈 문자열**이었다 (Part 1 §1).  `EXPID` 는 PRIMARY 로 중계되고 pair 양쪽이 다르면 변환이 멈춘다.

## 3. ics_sim 구현 영향 (구현 일감)

| 파일 | 변경 |
| --- | --- |
| `rawpair.py` | 선검사 루프(되감음 · 상한 100000회 → **D-018 로 1000000회**) 신설, clash 격리 로직 제거, `UNIQNAME` 제거, `ORIGNAME` 항상 기록 (**v1.6 에서 `EXPID` 로 대체 — 반영은 `ics-archon-v1.0-build` 몫**) |
| `state.py` | 확정 N 으로 카운터 동기화, **000000–999999 순환** (D-018, 2026-08-25 — 구 `099999`) · **`rewind_expnum()`** — 저장 못 한 프레임의 번호 기록 되감기 (raw spec 2.3절 8항, 2026-09-07. 반영은 `ics-archon-v1.0-build` 몫 — 이미 들어가 있다) |
| `sequencer._store()` | 확정된 이름만 수령 (이름 결정은 rawpair 몫) |
| `tests/test_raw_header.py` | `UNIQNAME` 필수 목록 제거·RETIRED 추가, `NAMECLSH` 시험 교체, 평시 `FILENAME`==`ORIGNAME` 불변식(**v1.6: `DETID` 필드 뗀 값 == `EXPID`**), 충돌·되감음·상한 시험 신설 |

---

## 관련 문서

| 문서 | 위치 |
| --- | --- |
| raw 헤더 카드 판정 원장 | [`KMT_CEU_Raw_FITS_Header_and_Refs_in_MEF_Converter_v1.20.md`](KMT_CEU_Raw_FITS_Header_and_Refs_in_MEF_Converter_v1.20.md) |
| 1위 준거 ICD | [`../mef_fits_spec/KMT_CEU_Science_MEF_ICD_L0AmpRaw_v4.3.md`](../mef_fits_spec/KMT_CEU_Science_MEF_ICD_L0AmpRaw_v4.3.md) — 구판 v4.2 는 `../mef_fits_spec/archive/` |
| MEF keyword 정의서 | [`../mef_fits_spec/KMT_CEU_MEF_FITS_Main_Keywords_Final_v1.1.md`](../mef_fits_spec/KMT_CEU_MEF_FITS_Main_Keywords_Final_v1.1.md) — 구판 v1.0 은 `../mef_fits_spec/archive/` |
| Converter | [`../mef_converter/kmt_ceu_archon_mknt_to_l0_amp_mef_v2_1.py`](../mef_converter/kmt_ceu_archon_mknt_to_l0_amp_mef_v2_1.py) (v2.5.0 — 파일명의 `_v2_1` 은 그대로이고 판은 `SOFTWARE_VERSION` 이 말한다) |
| **raw spec (현행)** | [`KMT_CEU_Raw_FITS_Specification_v1.14.md`](KMT_CEU_Raw_FITS_Specification_v1.14.md) — 구판(v1.2 구명 Pair_Spec 이후)은 `archive/` |
| 검출기 채널 ↔ AmpID 맵 | [`Detector_Ch_to_AmpID_Map_v1.1.txt`](Detector_Ch_to_AmpID_Map_v1.1.txt) — 기계 정본 (converter v2.5.0 이 내장 사본으로 대조) |
| 전신 문서 | `KMT_CEU_Raw_Header_Review_MEF_Impacts_v0.4.md` · `KMT_CEU_Raw_Numbering_and_Identity_v0.2.md` — `archive/` 에 없다.  git 이력과 운영자 외부 백업(`__backup_raw_fits_spec_oldver`)에 보존 (README) |
| 결정 기록 | [`../project_management/governance/DECISION_LOG.md`](../project_management/governance/DECISION_LOG.md) |
| 검토 진행 상태 | [`SMC_CLAUDE.md`](SMC_CLAUDE.md) |
